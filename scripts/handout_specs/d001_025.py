# -*- coding: utf-8 -*-
SPECS = {
    1: {
        "topic": "Cloud Computing & Azure Fundamentals",
        "subtitle": "Who holds the spatula, which city, which building",
        "phase": "1 - Azure & DevOps Foundations",
        "tables": [
            {
                "title": "Architecture A — shared responsibility (same app, different Azure product)",
                "columns": ["Layer", "IaaS (VM)", "PaaS (App Service)", "SaaS"],
                "rows": [
                    ["Azure type", "Microsoft.Compute/virtualMachines", "Microsoft.Web/sites, Microsoft.Web/sites/functions", "Microsoft 365, GitHub, Azure DevOps"],
                    ["Application / data", "You: app, disks, backups", "You: app + App Settings / Connection Strings", "Vendor: tenant data under their SLA"],
                    ["Runtime / middleware", "You: IIS, nginx, .NET, Node, Java", "Vendor: Kudu / platform stack you pick", "Vendor"],
                    ["OS / patching", "You: Windows/Linux Update, NSG, disks", "Vendor patches the worker", "Vendor"],
                    ["Network / identity", "You: VNet, NIC, NSG, public IP", "You: VNet integration, Private Endpoint, Entra ID", "Vendor + your tenant admin"],
                    ["Failure blast", "You reboot the VM; you own the 2am patch", "Swap slot / scale out; platform recycles workers", "Wait on vendor status + your admin settings"],
                    ["Lab example", "Standard_B1s in rg-day01-lab", "Free/F1 or B1 App Service plan — do not deploy yet", "dev.azure.com personal org (Day 5)"],
                ],
                "widths": [32, 50, 52, 48],
            },
            {
                "title": "Architecture B — geography map (pick a region like a latency + residency decision)",
                "columns": ["Concept", "Azure fact", "Lab default"],
                "rows": [
                    ["Geography", "Compliance boundary (data residency)", "India / nearest allowed geography"],
                    ["Region", "Group of datacenters; ARM location key e.g. centralindia", "centralindia (or nearest)"],
                    ["Paired region", "CAF: some regions pair for Azure-initiated GRB", "Note pair; do not dual-region a lab"],
                    ["Availability Zone", "Independent datacenter in the region (zone 1/2/3)", "Skip AZ SKUs for Day 1 — extra cost"],
                    ["Availability Set", "Fault + update domains inside one region (classic VM HA)", "Not needed for empty RG labs"],
                    ["Resource group", "Lifecycle + RBAC + cost scope, not a security boundary by itself", "rg-day01-lab"],
                    ["Subscription", "Billing + ARM quota + RBAC root for most labs", "Personal sub ID from portal.azure.com"],
                ],
                "widths": [38, 82, 62],
            },
            {
                "title": "Architecture C — pitfalls that burn money or pin you to the wrong city",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["Work tenant by habit", "Directory picker shows employer Entra ID", "Switch directory → personal Microsoft account only"],
                    ["No budget", "Invoice surprise after a VM left running", "Cost Management → Budgets → Rs 500 / $20 email alert"],
                    ["Wrong region forever", "ARM templates hard-code eastus; users in India wait", "Set location once; reuse in az group create -l"],
                    ["IaaS for a website", "You patch IIS on a VM for a hello-world", "PaaS App Service / Functions unless you need the OS"],
                    ["AZ as a personality trait", "Zone-redundant SKUs on an empty lab", "Single-region RG; AZ later when SLA requires it"],
                    ["Orphan RG", "rg-day01-lab sits empty for 90 days", "Delete if unused, or reuse as Day 2 target"],
                ],
                "widths": [42, 70, 70],
            },
        ],
        "one_liner": "IaaS is your kitchen, PaaS is a shared kitchen, SaaS is takeout — region is the city, zone is the building.",
        "lab_intro": "Personal Microsoft account and personal Azure subscription only. No work SSO, no employer tenant.",
        "lab": [
            "portal.azure.com → sign in with the personal Microsoft account. Top-right directory picker: confirm it is NOT a work tenant.",
            "Subscriptions → open the personal subscription → copy Name + Subscription ID into notes (never a work sub).",
            "Cost Management + Billing → Budgets → Add → amount Rs 500 or $20 → alert email = you. Save.",
            "Resource groups → Create → Name rg-day01-lab → Region Central India (or nearest) → Review + create.",
            "Open Cloud Shell (Bash) → az account show --output table → confirm the same subscription ID.",
            "Browse Create a resource → Storage account or App Service create blade. Read SKUs. Do NOT deploy.",
            "Optional: az group list --output table. Keep rg-day01-lab for Day 2 or delete if you created nothing inside.",
        ],
        "code_title": "Prove the personal subscription, then create the Day 1 RG",
        "code": "az account show --output table\naz account show --query \"{name:name, id:id, tenantId:tenantId}\" -o jsonc\naz group create --name rg-day01-lab --location centralindia\naz group list --output table\n",
        "checklist": [
            "Can explain IaaS vs PaaS vs SaaS with one Azure resource type each",
            "Can explain Region vs Availability Zone without saying 'datacenter somewhere'",
            "Budget alert exists on the personal subscription",
            "rg-day01-lab exists in the chosen region (or you can recreate it)",
        ],
        "tomorrow": "Azure Portal, CLI & PowerShell basics",
    },
    2: {
        "topic": "Azure Portal, CLI & PowerShell Basics",
        "subtitle": "Portal is the map; CLI is GPS; PowerShell is the object knife",
        "phase": "1 - Azure & DevOps Foundations",
        "tables": [
            {
                "title": "Architecture A — three clients, one ARM control plane",
                "columns": ["Hop", "What it is", "Proof in the lab"],
                "rows": [
                    ["You", "Intent: create RG, list, delete", "Decide rg-day02-lab / centralindia"],
                    ["Portal", "portal.azure.com ARM JSON via the blade", "Create RG once with clicks to feel the cost"],
                    ["Azure CLI", "az → REST to management.azure.com", "az group create --name rg-day02-lab"],
                    ["Azure PowerShell", "Az module cmdlets, PS objects", "New-AzResourceGroup when you need objects"],
                    ["ARM", "Auth, RBAC, policy, lock, provider routing", "Same 201/202 whether click or CLI"],
                    ["Resource provider", "Microsoft.Resources/resourceGroups", "az group show -n rg-day02-lab -o jsonc"],
                ],
                "widths": [32, 72, 78],
            },
            {
                "title": "Architecture B — pick the tool by the job, not by comfort",
                "columns": ["Job", "Default", "Move off that default when"],
                "rows": [
                    ["First look at a service", "Portal", "You already know the ARM type and SKU"],
                    ["Same task twice this week", "az script in a gist / repo", "You are still clicking the same blade"],
                    ["Put it in a pipeline later", "AzureCLI@2 or az in YAML", "Classic click-ops cannot be reviewed"],
                    ["Need objects / Windows estate", "PowerShell Az", "JSON text from az is enough"],
                    ["Naming", "rg-, st, app-, pip- prefixes", "You ship 'New Resource Group (1)'"],
                    ["Output", "az -o table for humans, jsonc for diffs", "You grep unstructured Portal screenshots"],
                ],
                "widths": [48, 58, 76],
            },
            {
                "title": "Architecture C — pitfalls that silently target the wrong subscription",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["az login to work tenant", "az account show lists employer sub", "az logout; az login; az account set --subscription <personal-id>"],
                    ["Cloud Shell vs local CLI", "Commands work in Portal shell, fail on laptop", "Install https://aka.ms/installazurecliwindows; az version"],
                    ["Default location drift", "RG in eastus because last session stuck", "Always pass --location / -l on create"],
                    ["Delete in Portal, create in CLI", "Two RGs, two names, two bills", "One name: rg-day02-lab; az group delete --yes --no-wait"],
                    ["PowerShell 5 vs 7 mix", "Connect-AzAccount missing Az.Accounts", "Install-Module Az -Scope CurrentUser"],
                    ["json vs table", "You cannot see properties you need", "az group show -o jsonc then --query"],
                ],
                "widths": [40, 71, 71],
            },
        ],
        "one_liner": "Explore once in Portal. Second time use CLI. Forever after, script it.",
        "lab_intro": "Personal Microsoft account and personal Azure subscription only. Install CLI on your own PC.",
        "lab": [
            "Install Azure CLI from https://aka.ms/installazurecliwindows (or brew/apt on your OS). Run az version.",
            "Terminal: az login → pick the personal subscription. az account set --subscription \"<personal-id>\".",
            "az group create --name rg-day02-lab --location centralindia. Confirm with az group show --name rg-day02-lab -o jsonc.",
            "portal.azure.com → Resource groups → Create the same shape once (name rg-day02-portal-compare) to count the clicks.",
            "az group list --output table. Compare properties: location, provisioningState, tags.",
            "az group delete --name rg-day02-portal-compare --yes --no-wait. Keep or delete rg-day02-lab before Day 3.",
            "Optional: pwsh → Connect-AzAccount → Get-AzResourceGroup -Name rg-day02-lab. Same ARM object, different client.",
        ],
        "code_title": "Login, pin the personal sub, create and inspect the RG",
        "code": "az login\naz account set --subscription \"<your-personal-subscription-id>\"\naz group create --name rg-day02-lab --location centralindia\naz group show --name rg-day02-lab --output jsonc\naz group list --query \"[?starts_with(name, 'rg-day0')].{name:name, loc:location, state:properties.provisioningState}\" -o table\naz group delete --name rg-day02-lab --yes --no-wait\n",
        "checklist": [
            "Can explain Portal vs CLI vs PowerShell in one line each",
            "az login targets the personal subscription (az account show)",
            "Created rg-day02-lab via CLI and inspected JSON",
            "Deleted the compare RG (or documented why it remains)",
        ],
        "tomorrow": "Azure Resource Manager (ARM) basics",
    },
    3: {
        "topic": "Azure Resource Manager (ARM) Basics",
        "subtitle": "Every Portal click is an ARM call — tags, locks, providers",
        "phase": "1 - Azure & DevOps Foundations",
        "tables": [
            {
                "title": "Architecture A — request path (you never talk to the VM first)",
                "columns": ["Layer", "ARM job", "Lab command / type"],
                "rows": [
                    ["Client", "Portal / az / PowerShell / SDK / pipeline", "az group create, az lock create"],
                    ["ARM control plane", "AuthN/Z, validate schema, orchestrate", "management.azure.com REST"],
                    ["Management group", "Policy + RBAC above subscriptions", "Skip for personal lab; know it exists"],
                    ["Subscription", "Quota, billing, RBAC assignment scope", "az account show"],
                    ["Resource group", "Deploy scope + delete scope + tag rollup", "Microsoft.Resources/resourceGroups"],
                    ["Resource provider", "Microsoft.Storage, Microsoft.Web, Microsoft.Compute", "az provider list --query [].namespace"],
                    ["Resource", "The billed object", "Do not create a VM today — RG + lock only"],
                ],
                "widths": [36, 72, 74],
            },
            {
                "title": "Architecture B — decide tag vs lock vs policy",
                "columns": ["Need", "Default", "Escalate when"],
                "rows": [
                    ["Cost + owner", "Tags Project=100Days Owner=personal Env=lab", "Finance needs a policy to deny untagged RGs"],
                    ["Stop accidental delete", "CanNotDelete lock on the RG", "Prod: lock + PIM + deny-delete policy"],
                    ["Stop all writes", "ReadOnly lock", "You still need to rotate secrets — unlock first"],
                    ["Repeatable deploy", "Bicep/ARM template later in the series", "Click-ops that cannot be reviewed in Git"],
                    ["Provider missing", "az provider register --namespace Microsoft.Web", "Portal create blade hangs on Registering"],
                    ["Cleanup", "Delete RG after removing locks", "Lock left on → az group delete fails forever"],
                ],
                "widths": [40, 72, 70],
            },
            {
                "title": "Architecture C — pitfalls that make delete hang or cost unowned",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["Lock then delete", "az group delete: Cannot delete resource group ... because of lock", "az lock delete --name cannot-delete -g rg-day03-lab"],
                    ["Tags on resource not RG", "Cost analysis cannot group by Project", "Tag the RG AND resources; inherit is not magic"],
                    ["CanNotDelete on a VM NIC", "RG delete still blocked by nested lock", "az lock list -g ... -o table; delete each"],
                    ["ReadOnly during lab", "az group update --tags fails", "Use CanNotDelete for this lab, not ReadOnly"],
                    ["Provider not registered", "Namespace Microsoft.Web not registered", "az provider register --namespace Microsoft.Web"],
                    ["Leaving the lock in prod-think", "Empty lab RG cannot be deleted next week", "Remove lock, then --yes --no-wait delete"],
                ],
                "widths": [40, 72, 70],
            },
        ],
        "one_liner": "Tags are sticky notes for Finance. Locks are duct tape so nobody deletes the lab by accident.",
        "lab_intro": "Personal subscription only. Create, lock, fail-delete, unlock, delete. Leave no billed resources.",
        "lab": [
            "az group create -n rg-day03-lab -l centralindia --tags Project=100Days Owner=personal Env=lab",
            "portal.azure.com → rg-day03-lab → Tags. Confirm the three tags. Cost Management → Cost analysis → Group by Tag: Project.",
            "az lock create --name cannot-delete --lock-type CanNotDelete --resource-group rg-day03-lab",
            "az group delete -n rg-day03-lab --yes  (expect failure while locked). Read the error; do not force anything else.",
            "az lock list --resource-group rg-day03-lab -o table → az lock delete --name cannot-delete --resource-group rg-day03-lab",
            "az provider list --query [].namespace -o tsv | more  (Windows: findstr Microsoft). Note Microsoft.Resources / Microsoft.Web.",
            "az group delete -n rg-day03-lab --yes --no-wait. Confirm az group show -n rg-day03-lab fails.",
        ],
        "code_title": "Tag the RG, lock it, prove delete fails, then unlock and destroy",
        "code": "az group create -n rg-day03-lab -l centralindia --tags Project=100Days Owner=personal Env=lab\naz lock create --name cannot-delete --lock-type CanNotDelete --resource-group rg-day03-lab\naz lock list --resource-group rg-day03-lab --output table\n# Expect failure while locked:\n# az group delete -n rg-day03-lab --yes\naz lock delete --name cannot-delete --resource-group rg-day03-lab\naz group delete -n rg-day03-lab --yes --no-wait\n",
        "checklist": [
            "Can explain ARM as the control plane behind Portal and CLI",
            "Applied Project/Owner/Env tags on rg-day03-lab",
            "Proved CanNotDelete blocks az group delete",
            "Removed the lock and deleted the RG",
        ],
        "tomorrow": "DevOps principles & culture",
    },
    4: {
        "topic": "DevOps Principles & Culture",
        "subtitle": "CALMS is the habit. DORA is the receipt.",
        "phase": "1 - Azure & DevOps Foundations",
        "tables": [
            {
                "title": "Architecture A — CALMS as a delivery system, not a poster",
                "columns": ["Letter", "What it looks like in Azure DevOps", "Red flag"],
                "rows": [
                    ["Culture", "Blameless notes in work items / PR comments", "First question is 'who deployed?'"],
                    ["Automation", "YAML pipeline on every main push (Phase 3)", "Click-ops at midnight in Portal"],
                    ["Lean", "Small PRs, WIP limit on Boards Doing column", "Big-bang Friday release branch"],
                    ["Measurement", "DORA four keys tracked weekly, even roughly", "Only metric is ticket count"],
                    ["Sharing", "docs/ in the repo + PR templates (Phase 2)", "One person knows the release ritual"],
                    ["Not CALMS", "Buying Azure DevOps seats with no pipeline", "The sticker on the laptop"],
                ],
                "widths": [28, 82, 72],
            },
            {
                "title": "Architecture B — DORA four keys (decide what you will actually count)",
                "columns": ["Metric", "Question", "Personal-lab proxy"],
                "rows": [
                    ["Deployment frequency", "How often do we ship to an env?", "Green pipeline runs to main per week"],
                    ["Lead time for changes", "Commit SHA → production how long?", "git log time → pipeline finish time"],
                    ["Change failure rate", "What % of releases need a hotfix?", "Reverts / failed releases / 'hotfix/' PRs"],
                    ["Time to restore (MTTR)", "How fast do we recover?", "Minutes from red pipeline to green on main"],
                    ["Elite vs theatre", "Daily deploys with tiny PRs", "Monthly release + 40% rollback is not elite"],
                    ["Agile vs DevOps", "Agile plans the work; DevOps ships it", "Standup without a pipeline is not DevOps"],
                ],
                "widths": [42, 62, 78],
            },
            {
                "title": "Architecture C — pitfalls that turn metrics into vanity",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["Deploy count without quality", "50 deploys, 20 hotfixes", "Pair frequency with change failure rate"],
                    ["Lead time = coding time", "Ignores PR wait + pipeline queue", "Measure commit to main merge to env"],
                    ["MTTR = mean time to first reply", "Slack 'looking' is not restore", "Clock until the failing check is green"],
                    ["CALMS as a job title", "Hire a 'DevOps engineer' to click Portal", "Team owns YAML, Boards, and the pager"],
                    ["No baseline", "Cannot say if Phase 3 CI helped", "Write today's 1-5 scorecard in the repo"],
                    ["Tool worship", "Azure DevOps org with zero pipelines", "DORA cares about outcomes, not hub count"],
                ],
                "widths": [42, 70, 70],
            },
        ],
        "one_liner": "DevOps is not a job title with a laptop sticker — CALMS + DORA tell you if the sticker is lying.",
        "lab_intro": "No Azure spend today. Personal notes in the repo you will create on Day 6 (or a local markdown file until then).",
        "lab": [
            "Create a local folder ~/azure-100-notes (or C:\\labs\\azure-100-notes). No work laptop policy files.",
            "Write docs/calms-dora-day04.md with a 1-5 score for Culture, Automation, Lean, Measurement, Sharing — last team or yourself, honestly.",
            "Circle the weakest letter. Write three sentences: the habit you will build in this 100-day series.",
            "Pick ONE DORA metric you can measure later (even 'pipeline runs on main per week'). Write the definition of done.",
            "Skim the Four Keys article (Google Cloud blog on DORA). Copy the four names into the markdown — no employer data.",
            "Optional: git init the notes folder locally so Day 11 has something real to commit. Do not push to a work remote.",
        ],
        "code_title": "Personal CALMS / DORA scorecard (commit this markdown later)",
        "code": "# docs/calms-dora-day04.md\n# CALMS (1-5, honest)\n# Culture: _\n# Automation: _\n# Lean: _\n# Measurement: _\n# Sharing: _\n# Weakest letter: _\n# DORA metric I will track: deployment_frequency | lead_time | change_failure_rate | time_to_restore\n# Proxy in this lab: count of green runs on main / week after Day 21\n# Baseline date: 2026-09-19\n",
        "checklist": [
            "Can recite CALMS without a slide",
            "Can name the four DORA metrics in order",
            "Filled a 1-5 scorecard and picked one metric to track",
            "No Azure resources created today (cost = 0)",
        ],
        "tomorrow": "Azure DevOps Services overview",
    },
    5: {
        "topic": "Azure DevOps Services Overview",
        "subtitle": "Five hubs pretending to be one product — plan, store, ship, prove, package",
        "phase": "1 - Azure & DevOps Foundations",
        "tables": [
            {
                "title": "Architecture A — idea to package (same work item ID if you wire it)",
                "columns": ["Step", "Hub", "Artifact you should see"],
                "rows": [
                    ["1 Plan", "Boards", "User Story / Bug with State, Area, Iteration"],
                    ["2 Store", "Repos", "Git commit SHA on refs/heads/main"],
                    ["3 Review", "Repos PRs", "PR linked to work item via AB#id"],
                    ["4 Ship", "Pipelines", "YAML run: jobs, steps, logs, artifacts"],
                    ["5 Prove", "Test Plans", "Test case + run result Pass/Fail/Blocked"],
                    ["6 Package", "Artifacts", "Feed views @local / @prerelease / @release"],
                    ["Identity", "Microsoft Entra ID / MSA", "Personal Microsoft account on the org"],
                ],
                "widths": [28, 40, 114],
            },
            {
                "title": "Architecture B — Azure DevOps vs GitHub (pick by constraint)",
                "columns": ["Constraint", "Azure DevOps", "GitHub"],
                "rows": [
                    ["Work items + TFVC history", "Boards + optional TFVC (you stay on Git)", "Issues; no TFVC"],
                    ["YAML CI + Classic", "Pipelines YAML + Classic editor still exists", "GitHub Actions YAML only"],
                    ["Private packages", "Azure Artifacts (NuGet/npm/Maven/Python/UPack)", "GitHub Packages"],
                    ["Manual test cases", "Test Plans (SKU-dependent)", "Not a first-class test-case product"],
                    ["Enterprise process", "Agile / Scrum / CMMI process templates", "Projects + issues; lighter process"],
                    ["This series", "Personal org + project azure-100-labs", "Optional public mirror later — not required"],
                ],
                "widths": [42, 70, 70],
            },
            {
                "title": "Architecture C — pitfalls that split the five hubs into five silos",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["Boards with no repo link", "Stories never mention a SHA", "Link commits/PRs with AB#<id>"],
                    ["Repo with no pipeline", "main is 'done' when someone copies ZIP", "Day 21: azure-pipelines.yml on main"],
                    ["Tests only in Slack", "'Looks good' with no Test Plans or CI test task", "Manual cases in Test Plans; unit tests in YAML"],
                    ["DLL in email", "No feed, no version, no restore", "Artifacts feed (Day 9) + nuget.config / .npmrc"],
                    ["Work org by accident", "dev.azure.com/employer-...", "Create a NEW personal org; never screenshot work"],
                    ["Public project too soon", "Lab secrets in wiki", "Create private project day05-overview"],
                ],
                "widths": [40, 71, 71],
            },
        ],
        "one_liner": "Boards plan, Repos store, Pipelines ship, Test Plans prove, Artifacts package — five apps, one org.",
        "lab_intro": "Personal org at https://dev.azure.com only. Name it something like yourname-100days. No employer org.",
        "lab": [
            "https://dev.azure.com → create organization (personal Microsoft account). Skip any work-directory invite.",
            "New project → Name day05-overview → Visibility Private → Process Agile → Create.",
            "Left nav → Boards → Work items → New User Story 'Touch all five hubs'. Save.",
            "Repos → Files → Initialize with a README. Note the clone URL https://dev.azure.com/<org>/day05-overview/_git/day05-overview",
            "Pipelines → Create Pipeline → Azure Repos Git → select the repo → inspect the YAML starter. Cancel without saving if you want Day 21 to be the first YAML.",
            "Test Plans hub → open once (enable trial if prompted). Artifacts → open once. Do not post screenshots of emails or tenant names.",
            "Bookmark https://dev.azure.com/<org>/day05-overview. Tomorrow you create the long-lived project azure-100-labs.",
        ],
        "code_title": "Hub map (bookmark) — optional Azure DevOps CLI later",
        "code": "# https://dev.azure.com/<your-org>/day05-overview\n# Hubs: Overview | Boards | Repos | Pipelines | Test Plans | Artifacts\n# Optional after Day 6:\n# az extension add --name azure-devops\n# az devops configure --defaults organization=https://dev.azure.com/<your-org> project=day05-overview\n# az boards work-item show --id <id> -o table\n",
        "checklist": [
            "Can name all five hubs and what each produces",
            "Personal org exists; project day05-overview is private Agile",
            "Opened Boards, Repos, Pipelines, Test Plans, Artifacts once",
            "No work org screenshots, no employer users invited",
        ],
        "tomorrow": "Setting up an Azure DevOps organization",
    },
    6: {
        "topic": "Setting Up an Azure DevOps Org",
        "subtitle": "Org is the tenancy. Project is the gym. Empty projects build no muscle.",
        "phase": "1 - Azure & DevOps Foundations",
        "tables": [
            {
                "title": "Architecture A — org stack (what each layer actually controls)",
                "columns": ["Layer", "Controls", "Lab choice"],
                "rows": [
                    ["Organization", "Users, OAuth apps, agent pools, billing, Policies", "Personal MSA owner only"],
                    ["Project", "Boards/Repos/Pipelines/Test/Artifacts isolation", "azure-100-labs (private)"],
                    ["Process", "Work item types + states (Agile/Scrum/Basic/CMMI)", "Agile for this series"],
                    ["Team", "Backlog, area, iteration, board columns", "Default project team is enough"],
                    ["Repo", "Git (default) vs TFVC (do not use TFVC here)", "Git, default branch main (Day 13)"],
                    ["Parallel jobs", "Microsoft-hosted vs self-hosted job SKUs", "Free Microsoft-hosted minute pool"],
                ],
                "widths": [32, 78, 72],
            },
            {
                "title": "Architecture B — permission bands (keep Collection Admins tiny)",
                "columns": ["Group", "Power", "Lab rule"],
                "rows": [
                    ["Project Collection Administrators", "All projects, org policies, user management", "You only. Nobody from work"],
                    ["Project Administrators", "One project's settings, repos, pipelines", "Fine — you are the lab owner"],
                    ["Contributors", "Work items, push (subject to policies), pipeline queue", "Default if you ever add a friend"],
                    ["Readers", "View boards/repos", "Stakeholders later, not today"],
                    ["Build Administrators", "Pipeline definitions / queues", "Do not grant to random MSA guests"],
                    ["Stakeholder vs Basic", "Stakeholder: limited Boards; Basic: Repos+Pipelines", "You need Basic on the personal account"],
                ],
                "widths": [52, 70, 60],
            },
            {
                "title": "Architecture C — pitfalls that mix work and lab forever",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["Invite work AAD users", "Employer shows in Org settings → Users", "Remove them. Personal MSA only"],
                    ["Public project", "Lab YAML and notes indexed", "Visibility = Private"],
                    ["CMMI because it sounded serious", "Change request types you will never use", "Agile process on create — cannot casually swap"],
                    ["One org per day", "Ten empty orgs, lost URLs", "One org; many projects if needed; home = azure-100-labs"],
                    ["CLI pointing at work org", "az devops project list shows client names", "az devops configure --defaults organization=https://dev.azure.com/<personal>"],
                    ["Collection Admin sprawl", "Guest with org-wide delete", "Audit Org settings → Permissions → PCA"],
                ],
                "widths": [42, 70, 70],
            },
        ],
        "one_liner": "Your Azure DevOps org is a gym membership — empty projects do not build muscle; daily reps do.",
        "lab_intro": "Personal Microsoft account only. Invite nobody from work. Project azure-100-labs is home for Days 7–100.",
        "lab": [
            "https://dev.azure.com/<your-org> → Organization settings (bottom left) → Overview. Note org name and owner email.",
            "Organization settings → Users. Confirm only your personal Microsoft account. Remove any work guest.",
            "Organization settings → Permissions → Project Collection Administrators. You only.",
            "New project → Name azure-100-labs → Private → Process Agile → Create.",
            "Project settings → Overview → Description: Personal 100DaysOfAzureDevOps labs - views are my own.",
            "az extension add --name azure-devops && az devops configure --defaults organization=https://dev.azure.com/<your-org> project=azure-100-labs",
            "az devops project list -o table && az devops project show --project azure-100-labs -o jsonc",
        ],
        "code_title": "Pin CLI defaults to the personal org and home project",
        "code": "az extension add --name azure-devops\naz devops login\naz devops configure --defaults organization=https://dev.azure.com/<your-org> project=azure-100-labs\naz devops project list --output table\naz devops project show --project azure-100-labs --output jsonc\n",
        "checklist": [
            "Can explain organization vs project vs process template",
            "azure-100-labs exists, private, Agile",
            "No work accounts in Org settings → Users",
            "az devops configure defaults point at this org + project",
        ],
        "tomorrow": "Azure Boards deep dive",
    },
    7: {
        "topic": "Azure Boards Deep Dive",
        "subtitle": "Epic to Task is a planning graph. WIP limits keep the board honest.",
        "phase": "1 - Azure & DevOps Foundations",
        "tables": [
            {
                "title": "Architecture A — Agile process work item graph",
                "columns": ["Type", "Parent / child", "This series example"],
                "rows": [
                    ["Epic", "Top of the tree", "100 Days Learning"],
                    ["Feature", "Child of Epic", "Phase 1 Foundations"],
                    ["User Story", "Child of Feature; AB# links to PRs later", "Explore Azure Test Plans"],
                    ["Task", "Child of Story; hours optional", "Create test plan, add 2 cases"],
                    ["Bug", "Can hang off Story or backlog", "Use when a lab step actually fails"],
                    ["Issue (Agile)", "Impediment, not a delivery item", "Skip unless blocked on SKU/trial"],
                    ["States", "New → Active → Resolved → Closed (Agile)", "Board maps these to To Do / Doing / Done"],
                ],
                "widths": [32, 70, 80],
            },
            {
                "title": "Architecture B — backlog vs board vs query",
                "columns": ["Surface", "Job", "Use it when"],
                "rows": [
                    ["Backlog", "Ordered list; drag rank", "Planning what might ship this phase"],
                    ["Board", "Columns = state; cards = stories", "Seeing WIP right now"],
                    ["Sprint / Iteration", "Timebox + capacity", "Optional for a 1-person lab"],
                    ["Query", "WIQL filter saved", "Work Item Type = User Story AND State <> Closed"],
                    ["Dashboard widget", "Chart from a query", "Day 10 Phase 1 Command Center"],
                    ["WIP limit", "Column policy on Doing", "Doing > 3 on a solo lab is theatre"],
                ],
                "widths": [36, 62, 84],
            },
            {
                "title": "Architecture C — pitfalls that turn Boards into sticky-note chaos",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["No hierarchy", "50 stories, zero Features", "Epic → Feature → Story before more cards"],
                    ["Everything in Doing", "Board is a parking lot", "Set WIP on Doing; finish or uncommit"],
                    ["Query = Excel export", "Stale CSV in Downloads", "Save shared query in azure-100-labs"],
                    ["Closing without moving", "State Closed, board still Doing", "Move the card; do not only edit State in the form"],
                    ["Work items in the wrong Area", "Default area vs team backlog empty", "Project settings → Teams → Area paths"],
                    ["Linking later 'when we have Git'", "Phase 2 PRs with no AB#", "Create stories now; link PRs from Day 14"],
                ],
                "widths": [40, 71, 71],
            },
        ],
        "one_liner": "Boards without WIP limits are sticky-note chaos with better fonts — limit Doing or the board owns you.",
        "lab_intro": "Project azure-100-labs. Personal org only. Agile process from Day 6.",
        "lab": [
            "Boards → Work items → New Work Item → Epic → Title 100 Days Learning → Save. Note the ID.",
            "New Feature Phase 1 Foundations → Add link → Parent → the Epic. Save.",
            "Create 3 User Stories as children of the Feature: Explore Azure Test Plans; Create Azure Artifacts feed; Stand up end-to-end mini project.",
            "Boards → Boards → drag one story New/To Do → Active/Doing → Closed/Done. Watch the column counts.",
            "Board settings (gear) → Columns → Doing → WIP limit 3. Save.",
            "Boards → Queries → New query → Work Item Type = User Story AND State <> Closed → Save as Open stories.",
            "Open one story → Related Work. You will link a PR here in Phase 2. Do not invent fake employer sprints.",
        ],
        "code_title": "Optional Azure Boards CLI — create a story with a parent Feature",
        "code": "az boards query --wiql \"SELECT [System.Id], [System.Title], [System.State] FROM WorkItems WHERE [System.WorkItemType] = 'User Story' AND [System.State] <> 'Closed'\" -o table\n# Create (IDs will differ):\n# az boards work-item create --type \"User Story\" --title \"Explore Azure Test Plans\" --project azure-100-labs\n",
        "checklist": [
            "Can explain Epic → Feature → User Story → Task",
            "Hierarchy exists in azure-100-labs",
            "One story moved to Done on the board; WIP set on Doing",
            "Saved query: User Story AND State <> Closed",
        ],
        "tomorrow": "Azure Test Plans basics",
    },
    8: {
        "topic": "Azure Test Plans Basics",
        "subtitle": "A shared checklist so 'works on my machine' stops being a personality trait",
        "phase": "1 - Azure & DevOps Foundations",
        "tables": [
            {
                "title": "Architecture A — Test Plans object model",
                "columns": ["Object", "What it is", "Lab name"],
                "rows": [
                    ["Test Plan", "Container for a milestone / sprint", "Day08 Smoke"],
                    ["Test Suite (static)", "Hand-picked cases", "Portal checks"],
                    ["Requirement-based suite", "Auto from a User Story", "Optional: suite from Day 7 story"],
                    ["Query-based suite", "WIQL of test cases", "Skip until you have 10+ cases"],
                    ["Test Case (work item)", "Steps: Action + Expected Result", "Login to Portal; Create RG via CLI"],
                    ["Test Run / Test Point", "Outcome: Passed, Failed, Blocked, Not executed", "Web runner, not a pipeline yet"],
                    ["Bug from fail", "Work item linked to the run", "File only if a step actually fails"],
                ],
                "widths": [40, 72, 70],
            },
            {
                "title": "Architecture B — when Test Plans vs tests in CI",
                "columns": ["Kind", "Where it runs", "Choose it when"],
                "rows": [
                    ["Unit / lint", "Pipeline task (dotnet test, npm test)", "Every commit; milliseconds-minutes"],
                    ["API / UI automation", "Pipeline or dedicated agent", "Regression you can script"],
                    ["Manual Test Case", "Test Plans web runner", "UAT, portal clicks, exploratory charter"],
                    ["Exploratory session", "Test & Feedback / exploratory testing", "You do not know the steps yet"],
                    ["Traceability", "Story ↔ Test Case ↔ Run ↔ Bug", "Audit: did this story get proven?"],
                    ["Do not", "Excel on a laptop named TestPlan_vFinal", "Nobody else can execute it"],
                ],
                "widths": [40, 62, 80],
            },
            {
                "title": "Architecture C — pitfalls that make Test Plans a graveyard",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["SKU wall", "Test Plans hub empty / paywall", "Basic + Test Plans trial, or Visual Studio subscriber; do not use work license"],
                    ["Cases with no steps", "Title only, runner is guesswork", "Action + Expected on every step"],
                    ["Never run", "100 cases, 0 outcomes", "Execute two cases today; mark Pass/Blocked"],
                    ["No link to Boards", "Cannot answer 'is the story tested?'", "Add link → User Story from Day 7"],
                    ["Blocked without reason", "Outcome Blocked, empty comment", "Comment: missing personal sub / CLI not installed"],
                    ["Automate later = never", "Manual forever for unit-level checks", "Phase 3: put scriptable tests in YAML"],
                ],
                "widths": [36, 72, 74],
            },
        ],
        "one_liner": "Automated tests verify what you expected. Test Plans track what a human must still prove.",
        "lab_intro": "Project azure-100-labs. Enable Test Plans on the personal org if the hub asks (trial / included SKU). No work Visual Studio license.",
        "lab": [
            "azure-100-labs → Test Plans. If prompted, start the Test Plans trial on this personal org only.",
            "New Test Plan → Name Day08 Smoke → Area = default team. Create.",
            "Add suite → Static suite → Portal checks.",
            "New Test Case: Login to Portal. Steps: (1) Open portal.azure.com (2) Confirm personal directory. Expected: subscription visible, no work tenant.",
            "New Test Case: Create RG via CLI. Steps: az group create -n rg-day08-smoke -l centralindia then az group delete -n rg-day08-smoke --yes --no-wait.",
            "Run for web application → mark Login Passed. Mark CLI case Passed or Blocked with a comment if az is missing.",
            "Open the test case → Links → add User Story Explore Azure Test Plans (Day 7). Boards should show the test link.",
        ],
        "code_title": "Test case steps (paste into the web runner) plus the CLI under test",
        "code": "# TC: Login to Portal\n# 1. Open portal.azure.com\n# 2. Directory picker = personal Microsoft account\n# Expected: subscription name + ID visible; no work tenant\n\n# TC: Create RG via CLI\naz group create --name rg-day08-smoke --location centralindia\naz group show --name rg-day08-smoke --query location -o tsv\naz group delete --name rg-day08-smoke --yes --no-wait\n",
        "checklist": [
            "Can explain Test Plan vs Suite vs Case vs Run",
            "Day08 Smoke exists with two cases and at least one executed outcome",
            "One case linked to a Boards User Story",
            "No work-org Test Plans used",
        ],
        "tomorrow": "Azure Artifacts",
    },
    9: {
        "topic": "Azure Artifacts",
        "subtitle": "The pantry. Pipelines cook. Without a feed you re-buy flour every build.",
        "phase": "1 - Azure & DevOps Foundations",
        "tables": [
            {
                "title": "Architecture A — feed, views, upstreams (same package id, different promotion)",
                "columns": ["Piece", "What it is", "Lab setting"],
                "rows": [
                    ["Feed", "Private registry scoped to org or project", "day09-packages, project-scoped"],
                    ["Upstream nuget.org", "Proxy + cache of public NuGet", "Enable on create"],
                    ["Upstream npmjs", "Proxy + cache of public npm", "Enable on create"],
                    ["View @local", "Default: every push lands here", "Do not skip; this is the inbox"],
                    ["View @prerelease / @release", "Promotion without changing the version", "Use in Phase 3, not today"],
                    ["Retention", "Max versions / days to keep", "Leave default; 2 GB free tier is enough"],
                    ["Identity", "Feed permissions: Feed Readers / Contributors", "You = Owner; invite nobody"],
                ],
                "widths": [36, 72, 74],
            },
            {
                "title": "Architecture B — client config by ecosystem (Phase 3 will consume this URL)",
                "columns": ["Ecosystem", "Config key / file", "Publish command later"],
                "rows": [
                    ["NuGet / .NET", "nuget.config packageSources", "dotnet nuget push --source <feed>"],
                    ["npm / Node", ".npmrc registry= + always-auth=true", "npm publish (with PAT/oauth)"],
                    ["Python", "pip index-url / .pypirc", "twine upload --repository-url"],
                    ["Maven", "pom.xml repository + settings.xml", "mvn deploy"],
                    ["Universal Packages", "az artifacts universal", "az artifacts universal publish"],
                    ["Pipeline restore", "NuGetCommand@2 / npm authenticate", "Never paste a PAT into YAML"],
                ],
                "widths": [36, 72, 74],
            },
            {
                "title": "Architecture C — pitfalls that republish secrets or miss the cache",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["Org-scoped feed by accident", "Every project sees lab packages", "Create as project-scoped to azure-100-labs"],
                    ["No upstream", "CI fails when nuget.org blips; also slower", "Include packages from common public sources"],
                    ["PAT in nuget.config committed", "Secret in git log", "Credential Provider / pipeline env; .gitignore nuget.config with keys"],
                    ["Emailing nupkgs", "MyLib_final_v2.zip", "That is the problem Artifacts exists to kill"],
                    ["@release without promotion", "Consumers see every local push", "Promote package version to @release in the UI"],
                    ["Empty feed panic", "0 packages today", "Fine — URL in notes is the deliverable"],
                ],
                "widths": [42, 70, 70],
            },
        ],
        "one_liner": "Artifacts are the pantry — Pipelines cook dinner; without a feed you keep re-buying the same flour every build.",
        "lab_intro": "Project azure-100-labs. Create an empty project-scoped feed. Do not publish secrets. Personal org only.",
        "lab": [
            "azure-100-labs → Artifacts → Create Feed.",
            "Name day09-packages. Scope: Project azure-100-labs (not organization). Visibility: members of this project.",
            "Check Include packages from common public sources (nuget.org, npmjs). Create.",
            "Feed settings → Views. Note @local, @prerelease, @release. Do not delete them.",
            "Connect to feed → NuGet → copy the packageSources URL. Paste into notes/artifacts-day09.md locally.",
            "Connect to feed → npm → copy the registry= https://pkgs.dev.azure.com/.../npm/registry/ line.",
            "Permissions tab: you are Owner. Do not add work users. Empty package list is success for today.",
        ],
        "code_title": "nuget.config + .npmrc pointing at the project feed (no PAT in Git)",
        "code": "<?xml version=\"1.0\" encoding=\"utf-8\"?>\n<configuration>\n  <packageSources>\n    <clear />\n    <add key=\"day09-packages\" value=\"https://pkgs.dev.azure.com/<org>/azure-100-labs/_packaging/day09-packages/nuget/v3/index.json\" />\n  </packageSources>\n</configuration>\n\n# .npmrc (project)\n# registry=https://pkgs.dev.azure.com/<org>/azure-100-labs/_packaging/day09-packages/npm/registry/\n# always-auth=true\n",
        "checklist": [
            "Can explain feed vs upstream vs view (@local/@release)",
            "day09-packages exists, project-scoped, upstreams on",
            "Feed URL saved in notes (NuGet and/or npm)",
            "No PAT, password, or nupkg committed",
        ],
        "tomorrow": "Mini project + recap",
    },
    10: {
        "topic": "Mini Project + Recap (Phase 1)",
        "subtitle": "Five hubs + ARM hygiene in one personal project before Git gets serious",
        "phase": "1 - Azure & DevOps Foundations",
        "tables": [
            {
                "title": "Architecture A — Phase 1 map (what must exist before Day 11)",
                "columns": ["Track", "Days", "Proof in azure-100-labs"],
                "rows": [
                    ["Cloud models", "1", "Can name IaaS/PaaS/SaaS + region vs AZ"],
                    ["Clients + ARM", "2–3", "az group + tags/locks understood; lab RGs gone"],
                    ["CALMS / DORA", "4", "docs/calms-dora-day04.md (local or in repo)"],
                    ["Five hubs", "5–9", "Boards tree, Test Plan, Artifacts feed"],
                    ["Org hygiene", "6", "Personal org; PCA = you; project private Agile"],
                    ["Git bootstrap", "10", "README on default branch; clone URL works"],
                    ["Cost", "1–3 leftover", "az group list has no rg-day0* labs"],
                ],
                "widths": [32, 28, 122],
            },
            {
                "title": "Architecture B — hub wiring for Phase 2/3 (decide what you will not redo)",
                "columns": ["Hub", "Phase 1 state", "Do not invent later"],
                "rows": [
                    ["Boards", "Epic + Feature + stories + WIP", "A second parallel backlog"],
                    ["Repos", "Git repo + README", "TFVC 'because enterprise'"],
                    ["Pipelines", "Hub opened; YAML waits until Day 21", "Classic release for labs"],
                    ["Test Plans", "Day08 Smoke executed", "Excel test matrix"],
                    ["Artifacts", "day09-packages + upstreams", "Checking in bin/*.dll"],
                    ["Dashboard", "Query chart of open stories", "Screenshot-only status"],
                ],
                "widths": [32, 72, 78],
            },
            {
                "title": "Architecture C — pitfalls that fake a capstone",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["Empty README", "Initialize clicked, still 'add some code'", "Commit a real README.md via Git or Files UI"],
                    ["Repo in day05-overview only", "Home project has no Git", "Use azure-100-labs as the canonical repo"],
                    ["Orphan Azure RGs", "rg-day01-lab still billed", "az group delete leftover rg-day0*"],
                    ["Dashboard on work org", "Widgets show employer queries", "Delete it; recreate in personal org"],
                    ["Public README with sub IDs", "Subscription GUID on the internet", "No IDs, no PATs, no email lists"],
                    ["Skipping Boards because Git is coming", "Phase 2 PRs with no AB#", "Keep the Epic; you will link from Day 14"],
                ],
                "widths": [42, 70, 70],
            },
        ],
        "one_liner": "Phase 1 recap: cloud models, Portal/CLI, ARM, CALMS/DORA, and the five hubs — foundations before pipelines.",
        "lab_intro": "Personal project azure-100-labs. Initialize Git, dashboard, and cost cleanup. No employer remotes.",
        "lab": [
            "Boards: confirm Epic 100 Days Learning, Feature Phase 1 Foundations, at least 3 stories. Fix if missing.",
            "Repos → Files. If empty: Initialize with a README. If clone exists, skip to the git commands below.",
            "Locally: git clone https://dev.azure.com/<org>/azure-100-labs/_git/azure-100-labs && add README content && git push.",
            "Overview → Dashboards → New dashboard Phase 1 Command Center → add Chart for work items using query Open stories.",
            "Artifacts: confirm feed day09-packages. Test Plans: confirm Day08 Smoke still exists.",
            "az group list --query \"[?starts_with(name, 'rg-day')].{name:name, loc:location}\" -o table → delete leftovers.",
            "Write 5 lines in README: what stuck, what was confusing. Personal voice. No client names.",
        ],
        "code_title": "Bootstrap the lab repo and audit leftover resource groups",
        "code": "git clone https://dev.azure.com/<org>/azure-100-labs/_git/azure-100-labs\ncd azure-100-labs\nprintf '%s\\n' '# Azure 100 Labs' 'Personal learning repo for #100DaysOfAzureDevOps' > README.md\ngit add README.md\ngit commit -m \"docs: bootstrap lab repo\"\ngit push -u origin HEAD\naz group list --query \"[?starts_with(name, 'rg-day')].name\" -o tsv\n",
        "checklist": [
            "Can recap IaaS/PaaS/SaaS, ARM tags/locks, CALMS, DORA, five hubs",
            "azure-100-labs has Git README + Boards tree + Artifacts feed + Test Plan",
            "Dashboard widget exists from a real query",
            "No leftover rg-day* resource groups (or listed why kept)",
        ],
        "tomorrow": "Git fundamentals",
    },
    11: {
        "topic": "Git Fundamentals",
        "subtitle": "Working tree, index, commit DAG, remote — four areas, one SHA",
        "phase": "2 - Azure Repos & Git Mastery",
        "tables": [
            {
                "title": "Architecture A — four Git areas (the system map)",
                "columns": ["Area", "On disk", "Command that moves data"],
                "rows": [
                    ["Working tree", "Your files (notes/day11.md)", "edit; git status shows unstaged"],
                    ["Index / staging", ".git/index", "git add notes/day11.md"],
                    ["Local repo", ".git/objects commit/tree/blob", "git commit -m \"docs: ...\""],
                    ["Remote", "Azure Repos refs/heads/*", "git push -u origin feature/day11-notes"],
                    ["HEAD", "Pointer to branch or detached SHA", "git switch / git checkout"],
                    ["Ref", ".git/refs/heads/main (41-byte SHA + newline)", "git branch -v"],
                    ["Stash (optional)", ".git/refs/stash", "Do not stash as a backup strategy"],
                ],
                "widths": [36, 70, 76],
            },
            {
                "title": "Architecture B — object model (decide what a commit actually is)",
                "columns": ["Object", "Contains", "Why CI cares"],
                "rows": [
                    ["Blob", "File bytes, SHA-1 (or SHA-256 if enabled)", "Content-addressed; rename is cheap"],
                    ["Tree", "Names + modes + child SHAs", "Pipeline checks out this tree"],
                    ["Commit", "tree + parent(s) + author + message", "Build reason: IndividualCI on that SHA"],
                    ["Tag", "Annotated tag object or lightweight ref", "Release tags later; not required today"],
                    ["Branch", "Movable ref, not a copy of files", "feature/* isolation without new repos"],
                    ["Merge vs fast-forward", "Extra commit with 2 parents vs pointer move", "Day 15; today prefer linear local merge"],
                ],
                "widths": [36, 72, 74],
            },
            {
                "title": "Architecture C — pitfalls that rewrite the wrong universe",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["Commit -a by reflex", "Unrelated files in the SHA", "git add -p / add the path; git status first"],
                    ["Push to work remote", "origin URL is employer", "git remote -v; origin must be personal Azure Repos"],
                    ["Empty commits as save points", "Noise in git log --oneline", "Commit when the note is a complete thought"],
                    ["Main for experiments", "Broken README on default branch", "git switch -c feature/day11-notes"],
                    ["Line-ending war", "Whole-file diffs on Windows", "core.autocrlf true on Win; .gitattributes later"],
                    ["git add . at repo root", "os_user credentials in .env", "No .env; Day 13 .gitignore"],
                ],
                "widths": [40, 71, 71],
            },
        ],
        "one_liner": "Git is time travel with extra anxiety — commits are save points; branches are parallel universes you can still mess up.",
        "lab_intro": "Personal clone of azure-100-labs only. origin = https://dev.azure.com/<org>/azure-100-labs/_git/azure-100-labs",
        "lab": [
            "git clone (if needed) && git remote -v. Confirm origin is the personal Azure Repos URL, not a work GitHub.",
            "git status && git switch -c feature/day11-notes",
            "mkdir notes (Windows: mkdir notes) && write notes/day11.md with the four-area map in your words.",
            "git add notes/day11.md && git status (should show staged, not a pile of junk).",
            "git commit -m \"docs: day 11 git fundamentals notes\"",
            "git log --oneline --decorate --graph -5 && git push -u origin feature/day11-notes",
            "Azure Repos → Branches → see feature/day11-notes. Optional: merge locally to main or wait for Day 14 PR.",
        ],
        "code_title": "Branch, commit one file, push with upstream tracking",
        "code": "git status\ngit switch -c feature/day11-notes\nmkdir notes 2>nul\necho Day 11: working tree - index - commit - remote > notes/day11.md\ngit add notes/day11.md\ngit commit -m \"docs: day 11 git fundamentals notes\"\ngit log --oneline --decorate --graph -5\ngit push -u origin feature/day11-notes\n",
        "checklist": [
            "Can name working tree, index, local repo, remote",
            "feature/day11-notes exists on Azure Repos with one intentional commit",
            "git log --oneline shows the SHA",
            "origin is the personal azure-100-labs repo",
        ],
        "tomorrow": "Branching strategies",
    },
    12: {
        "topic": "Branching Strategies",
        "subtitle": "Git Flow is a seating chart. Trunk-based is a food truck. Pick for ship cadence.",
        "phase": "2 - Azure Repos & Git Mastery",
        "tables": [
            {
                "title": "Architecture A — three models (same Git, different branch lifetime)",
                "columns": ["Model", "Refs you keep", "Merge / release"],
                "rows": [
                    ["Git Flow", "main + develop + feature/* + release/* + hotfix/*", "release/* cut; merge to main AND develop"],
                    ["GitHub Flow", "main + short feature/*", "PR to main; deploy main"],
                    ["Trunk-based", "main; branches hours not days; maybe release/* tags", "Small PRs; feature flags if needed"],
                    ["This lab", "main protected (Day 19) + feature/* < 2 days", "Squash PR; no develop branch"],
                    ["TFVC / release branches forever", "long-lived release/2024.09", "Out of scope — you stay on Git"],
                    ["Environment branches (dev/qa/prod)", "Three eternally diverging mains", "Avoid; promote artifacts not branches"],
                ],
                "widths": [36, 78, 68],
            },
            {
                "title": "Architecture B — choose by constraint (not by blog title)",
                "columns": ["Constraint", "Default for this series", "Choose Git Flow when"],
                "rows": [
                    ["Ship cadence", "GitHub Flow / trunk-lite: many small PRs", "Hard monthly cut with 2+ supported versions"],
                    ["Team size", "1 person + future PR policy", "Large role-split (dev vs release managers)"],
                    ["CI maturity", "Need YAML on main (Day 21+)", "You cannot run CI on every feature branch"],
                    ["Hotfix", "PR straight to main, then patch", "You already maintain release/* lines"],
                    ["DORA lead time", "Hours: feature/* lived < 2 days", "Weeks: develop is a parking lot"],
                    ["Azure Repos policies", "Require PR to main (Day 19)", "Policies on develop AND main (double gates)"],
                ],
                "widths": [36, 78, 68],
            },
            {
                "title": "Architecture C — pitfalls that recreate Git Flow by accident",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["develop 'just in case'", "PRs to develop; main is months behind", "Delete develop; protect main only"],
                    ["feature/* weeks long", "Merge conflicts vs 40 files", "Integrate daily; smaller stories on Boards"],
                    ["Environment branches", "dev/qa/prod all have unique commits", "One main; pipelines deploy by stage"],
                    ["Hotfix off an old SHA you cannot find", "git log archaeology at 2am", "Tag releases; hotfix from that tag"],
                    ["No ADR", "Slack argument next month", "docs/branching-strategy.md in the repo"],
                    ["Force-push main", "Rewritten SHAs, broken pipelines", "Deny force push (Day 17/19)"],
                ],
                "widths": [40, 71, 71],
            },
        ],
        "one_liner": "Git Flow is a wedding seating chart; trunk-based is a food truck — pick complexity that matches how often you ship.",
        "lab_intro": "Personal azure-100-labs. Write an ADR. Do not create a develop branch 'for realism'.",
        "lab": [
            "git switch main && git pull && git switch -c feature/day12-branching-adr",
            "Create docs/branching-strategy.md with Decision: GitHub Flow (trunk-based lite).",
            "Document: main always deployable; feature/* < 2 days; PR required; squash merge; no develop/release/hotfix long lives.",
            "List 3 reasons Git Flow is rejected for this lab (solo, no multi-version prod, want short lead time).",
            "git add docs/branching-strategy.md && git commit -m \"docs: add branching strategy ADR\" && git push -u origin feature/day12-branching-adr",
            "Azure Repos → Pull requests → New PR into main. Title: docs: branching ADR. You may complete it or leave for Day 14 template.",
            "Repos → Branches. Confirm there is no develop. If you created one by habit, delete it after merging any needed commits.",
        ],
        "code_title": "ADR skeleton committed on a short-lived feature branch",
        "code": "# docs/branching-strategy.md\n# Decision: GitHub Flow (trunk-based lite)\n# - main is always deployable\n# - feature/* lives < 2 days\n# - PR required; squash merge\n# - No long-lived develop, release/*, or hotfix/* in this lab\n# - Environment promotion happens in pipelines, not extra branches\n",
        "checklist": [
            "Can contrast Git Flow vs GitHub Flow vs trunk-based with branch names",
            "docs/branching-strategy.md is on a feature branch (or merged)",
            "No develop branch in azure-100-labs",
            "ADR says squash + short feature/*",
        ],
        "tomorrow": "Azure Repos setup",
    },
    13: {
        "topic": "Azure Repos Setup",
        "subtitle": "Default branch, folder contract, .gitignore — stop future archaeology",
        "phase": "2 - Azure Repos & Git Mastery",
        "tables": [
            {
                "title": "Architecture A — repo layout (paths pipelines will look for)",
                "columns": ["Path", "Owns", "Later consumer"],
                "rows": [
                    ["/src", "Application code", "Day 24 SampleApi, Day 25 sample-node"],
                    ["/docs", "ADRs, notes", "Branching ADR, CALMS scorecard"],
                    ["/pipelines", "YAML entrypoints", "dotnet-ci.yml, node-ci.yml"],
                    ["/infra", "Bicep/ARM later", "Empty .gitkeep is enough today"],
                    ["/notes", "Daily markdown", "day11.md already"],
                    ["/.gitignore", "What never becomes a blob", "bin/, node_modules/, .env"],
                    ["README.md", "How we use Git here (Day 20)", "Clone instructions, no secrets"],
                ],
                "widths": [32, 62, 88],
            },
            {
                "title": "Architecture B — default branch + remote settings",
                "columns": ["Setting", "Default for labs", "Change it when"],
                "rows": [
                    ["Default branch", "main", "Legacy master still sitting there — rename"],
                    ["Clone URL", "https://dev.azure.com/<org>/azure-100-labs/_git/azure-100-labs", "SSH if you already have keys"],
                    ["Repo name vs project", "Can match project or not", "Multiple repos: name by product, not 'code'"],
                    ["README / gitignore on create", "Initialize with README", "Empty repo blocks some pipeline UIs"],
                    ["File size", "Keep under Azure Repos blob limits; no ISOs", "Git LFS only if you truly need binaries"],
                    ["Policies", "Overview today; enforce Day 19", "Do not skip policies because 'solo'"],
                ],
                "widths": [36, 82, 64],
            },
            {
                "title": "Architecture C — pitfalls that pollute the object database",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["Commit node_modules", "10k files, unreviewable PR", ".gitignore + git rm -r --cached node_modules"],
                    [".env in history", "PAT sitting in a blob forever", "gitignore now; rotate the PAT; filter-repo later if needed"],
                    ["Default still master", "Pipelines trigger: main never fires", "Repos → Branches → Change default to main"],
                    ["Windows bin/Debug", "dlls in every PR", "VisualStudio gitignore template"],
                    ["Root junk", "random.pdf, Desktop.ini", "Folders: src docs pipelines infra"],
                    ["Second remote named work", "git push ships lab notes to employer", "git remote remove work"],
                ],
                "widths": [40, 71, 71],
            },
        ],
        "one_liner": "A messy repo root is a messy brain — /src /docs /pipelines /infra saves future-you from archaeology.",
        "lab_intro": "Personal azure-100-labs. Set main as default. Add folder contract + .gitignore. No work remotes.",
        "lab": [
            "Repos → Branches. If default is master: … → Set as default branch on main. Update any local: git switch main.",
            "git switch -c feature/day13-repo-layout",
            "Create folders src, docs, pipelines, infra with .gitkeep files so Git tracks empty dirs.",
            "Add a .gitignore (Node + .NET + Python combined is fine). Must include node_modules/, bin/, obj/, .env, *.user.",
            "git add src docs pipelines infra .gitignore && git commit -m \"chore: repo layout and gitignore\" && git push -u origin feature/day13-repo-layout",
            "Open PR to main (or merge if policies are still off). Confirm default branch is main after merge.",
            "Project settings → Repositories → azure-100-labs → Overview. Note default branch = main. Policies come Day 19.",
        ],
        "code_title": ".gitignore for this lab (Node, .NET, Python, secrets)",
        "code": "node_modules/\ndist/\nbin/\nobj/\n*.user\n*.suo\n.env\n.env.*\n!.env.example\n__pycache__/\n.venv/\n*.log\n.DS_Store\nThumbs.db\n",
        "checklist": [
            "Default branch is main",
            "src, docs, pipelines, infra exist on the branch/main",
            ".gitignore blocks node_modules, bin/obj, .env",
            "origin is still the personal Azure Repos URL",
        ],
        "tomorrow": "Pull requests & code review",
    },
    14: {
        "topic": "Pull Requests & Code Review",
        "subtitle": "A PR without a description is a mystery novel whose murderer is 'I was in a hurry'",
        "phase": "2 - Azure Repos & Git Mastery",
        "tables": [
            {
                "title": "Architecture A — PR as a product (Azure Repos Git)",
                "columns": ["Piece", "Field / location", "Lab value"],
                "rows": [
                    ["Source → target", "feature/* → main", "Never PR into a random long-lived branch"],
                    ["Title", "Conventional: docs: / feat: / fix:", "Matches commit intent"],
                    ["Description", ".azuredevops/pull_request_template.md", "What / Why / Test plan / Risk"],
                    ["Work item link", "AB#123 in description or Development link", "Day 7 User Story"],
                    ["Reviewers", "Required reviewers (Day 19 policy)", "Yourself is OK on a personal org"],
                    ["Checks", "Build validation later (YAML)", "None yet — still review the diff"],
                    ["Merge type", "Squash (ADR Day 12)", "Avoid merge commit spaghetti in this lab"],
                ],
                "widths": [32, 74, 76],
            },
            {
                "title": "Architecture B — review etiquette (decide what blocks merge)",
                "columns": ["Finding", "Block the PR?", "Comment style"],
                "rows": [
                    ["Secret / PAT / sub ID", "Yes", "Do not paste the secret again; rotate"],
                    ["Wrong default target", "Yes", "Change target to main"],
                    ["Missing work item", "Yes after Day 19 policy", "Link AB# now even if policy is off"],
                    ["Nit: extra newline", "No", "Nit: prefix; do not hold merge"],
                    ["Unrelated refactor", "Yes if it hides the actual change", "Split PR"],
                    ["No test plan in template", "Yes for this series", "Tick local build / what you ran"],
                ],
                "widths": [42, 58, 82],
            },
            {
                "title": "Architecture C — pitfalls that rubber-stamp main",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["Empty template", "PR body is the file headings only", "Fill What/Why; delete unused sections"],
                    ["Complete without review", "Set auto-complete + ignore comments", "Read Files tab; comment on the hunk"],
                    ["Link to a work item on another project", "AB# does not resolve", "Use azure-100-labs IDs"],
                    ["PR from main to main", "Empty diff", "Always branch first"],
                    ["Screenshot of work board", "Employer story titles in the PR", "Personal org only; no work attachments"],
                    ["Force-complete with failures (later)", "Red YAML ignored", "Day 19 build validation required"],
                ],
                "widths": [44, 68, 70],
            },
        ],
        "one_liner": "A PR without a description is a mystery novel where the murderer is 'I was in a hurry'.",
        "lab_intro": "Personal azure-100-labs. Add a PR template, open a real PR, link a work item. No employer reviewers.",
        "lab": [
            "git switch main && git pull && git switch -c feature/day14-pr-template",
            "Create .azuredevops/pull_request_template.md with What / Why / Test plan / Risk (see code).",
            "git add .azuredevops/pull_request_template.md && git commit -m \"docs: add pull request template\" && git push -u origin feature/day14-pr-template",
            "Azure Repos → Pull requests → New pull request → source feature/day14-pr-template → target main.",
            "Confirm the template auto-fills. Complete What/Why. Link a Day 7 User Story (Development / work items).",
            "Files tab → self-review: title, description, no secrets. Add one comment on the markdown if you want the muscle memory.",
            "Complete with squash merge (if allowed) or leave open until Day 19 policies. Delete source branch on complete.",
        ],
        "code_title": ".azuredevops/pull_request_template.md",
        "code": "## What\n-\n## Why\n-\n## Test plan\n- [ ] Local `git status` clean on the branch\n- [ ] Linked work item (AB#)\n- [ ] No `.env`, PAT, or subscription GUID in the diff\n## Risk\n- Low / Med / High\n",
        "checklist": [
            "PR template exists under .azuredevops/",
            "Opened a PR into main with What/Why filled",
            "Work item linked (AB# or Development link)",
            "Self-reviewed the Files tab; no secrets",
        ],
        "tomorrow": "Advanced Git",
    },
    15: {
        "topic": "Advanced Git",
        "subtitle": "Rebase rewrites history. Merge keeps the plot twists. Know who is watching the timeline.",
        "phase": "2 - Azure Repos & Git Mastery",
        "tables": [
            {
                "title": "Architecture A — integrating feature/* onto main (same files, different graph)",
                "columns": ["Operation", "Graph result", "Safe on"],
                "rows": [
                    ["Merge commit", "New commit with two parents", "Shared branches if policy allows"],
                    ["Squash merge (ADO PR)", "One new commit on main; feature SHAs dropped", "feature/* → main (this lab default)"],
                    ["Rebase onto main", "Replay your commits; new SHAs", "Local feature/* only, then force-with-lease"],
                    ["Fast-forward", "Pointer move, no extra commit", "When feature is linear on main"],
                    ["Cherry-pick", "Copy a commit SHA onto another branch", "Hotfix one commit, not a whole feature"],
                    ["Soft reset", "Move branch, keep index/worktree", "Rewrite last local commit before push"],
                    ["Hard reset", "Move branch, destroy uncommitted (and matching files)", "Never on shared main"],
                ],
                "widths": [40, 78, 64],
            },
            {
                "title": "Architecture B — decide rebase vs merge vs squash",
                "columns": ["Situation", "Choose", "Do not"],
                "rows": [
                    ["Unpushed local commits, main moved", "git fetch; git rebase origin/main", "rebase origin/main after teammates pulled your branch"],
                    ["PR into main in this lab", "Squash (ADR)", "Merge commit unless you need the DAG"],
                    ["Public main / already shipped SHA", "Revert or forward fix", "rebase -i of origin/main"],
                    ["One commit actually belongs elsewhere", "cherry-pick <sha>", "merge the whole unrelated branch"],
                    ["Conflict during rebase", "edit, git add, git rebase --continue", "git rebase --abort only after you understand the conflict"],
                    ["Need to update a remote feature after rebase", "git push --force-with-lease", "git push --force (overwrites others)"],
                ],
                "widths": [52, 70, 60],
            },
            {
                "title": "Architecture C — pitfalls that delete someone else's Thursday",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["Rebase shared main", "Everyone's PRs cannot FF; CI SHAs vanish", "Never rebase main; revert forward"],
                    ["git push --force", "Teammate's commit disappeared from the branch", "--force-with-lease or new branch"],
                    ["Conflict markers committed", "<<<<<<< HEAD in source", "Resolve; git add; continue; grep for <<<<<<<"],
                    ["Cherry-pick a merge commit blindly", "Wrong tree", "cherry-pick -m 1 only if you mean it"],
                    ["Reset --hard after stashless work", "File gone from disk", "reflog: git reflog / git reset --hard ORIG_HEAD quickly"],
                    ["Squash + rebase both 'to be clean'", "Unrecognizable history twice", "Pick one strategy per PR"],
                ],
                "widths": [42, 70, 70],
            },
        ],
        "one_liner": "Rebase rewrites history; merge preserves the plot twists — choose based on whether anyone else is watching that timeline.",
        "lab_intro": "Personal repo only. Manufacture a conflict, resolve it, rebase a feature onto main. Do not force-push main.",
        "lab": [
            "git switch main && git pull. Create feature/day15-a and feature/day15-b from the same SHA.",
            "On day15-a: edit docs/day15-conflict.md line 1 to 'alpha'. Commit. Push.",
            "On day15-b: edit the same file line 1 to 'bravo'. Commit.",
            "git fetch origin && git rebase origin/main (if main moved) then rebase/merge day15-a into day15-b until conflict appears.",
            "Resolve conflict in the editor. git add docs/day15-conflict.md && git rebase --continue (or git commit if you used merge).",
            "If you rebased a branch already on origin: git push --force-with-lease. Never push --force to main.",
            "Write 5 lines in docs/day15-when.md: when you choose merge commit vs squash vs rebase. Commit on the feature branch.",
        ],
        "code_title": "Rebase a feature onto main, then push with lease",
        "code": "git fetch origin\ngit switch feature/day15-b\ngit rebase origin/main\n# fix conflicts → git add docs/day15-conflict.md → git rebase --continue\n# abort if lost: git rebase --abort\ngit push --force-with-lease origin feature/day15-b\n",
        "checklist": [
            "Can explain rebase vs merge vs squash with the parent graph",
            "Resolved a real conflict (markers gone)",
            "Used --force-with-lease on a feature branch, never on main",
            "Wrote when-to-choose notes in the repo",
        ],
        "tomorrow": "Git hooks & pre-commit",
    },
    16: {
        "topic": "Git Hooks & Pre-commit Checks",
        "subtitle": "The bouncer at the commit club — ugly messages do not pass the rope",
        "phase": "2 - Azure Repos & Git Mastery",
        "tables": [
            {
                "title": "Architecture A — where the check runs (client vs server)",
                "columns": ["Hook / check", "Runs on", "Can be bypassed?"],
                "rows": [
                    ["commit-msg (client)", "git commit, reads $1 message file", "Yes: --no-verify"],
                    ["pre-commit (client)", "Before the commit object exists", "Yes: --no-verify"],
                    ["pre-push (client)", "Before objects send to origin", "Yes: --no-verify"],
                    ["pre-commit.com framework", "Dev machine via .pre-commit-config.yaml", "Yes if not installed"],
                    ["Azure Repos branch policy", "Server: PR cannot complete", "No (unless you are admin bypass)"],
                    ["Pipeline build validation", "CI job on the PR", "No if required on the branch"],
                    ["Server-side hooks on Azure Repos", "Not your custom .git/hooks", "Do not expect upload of commit-msg to ADO"],
                ],
                "widths": [44, 72, 66],
            },
            {
                "title": "Architecture B — decide client hook vs policy vs CI",
                "columns": ["Goal", "Default", "Escalate when"],
                "rows": [
                    ["Conventional subject line", "commit-msg hook in this lab", "Team will skip --no-verify: add PR title check too"],
                    ["Secret scan", "pre-commit detect-secrets / gitleaks locally", "Also pipeline + never rely on honor system"],
                    ["Format / lint", "pre-commit ruff/prettier", "Same check in YAML so PRs from a phone still fail"],
                    ["Block force-push main", "Server permission + policy (Day 17/19)", "A client hook cannot stop another laptop"],
                    ["Commit prefix dayNN:", "Optional lab hook", "Do not require this on real product repos"],
                    ["Windows execution bit", "hooks as .sample until copied", "Git for Windows: hook must be executable / sh"],
                ],
                "widths": [40, 72, 70],
            },
            {
                "title": "Architecture C — pitfalls that make hooks folklore",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["Hook only in .git/hooks", "Clone on laptop 2 has no bouncer", "Commit a sample to docs/ + pre-commit config in Git"],
                    ["--no-verify as muscle memory", "Bad messages still on main", "Branch policy on PR title; CI required"],
                    ["CRLF breaks shebang", "hook: command not found", "Save LF; #!/bin/sh; Git Bash on Windows"],
                    ["Hook prints secrets on fail", "PAT in the terminal scrollback", "Message = 'invalid format', not the file body"],
                    ["pre-commit without rev pin", "Upstream hook changed behavior overnight", "Pin sha in .pre-commit-config.yaml"],
                    ["Expecting Azure Repos to run your commit-msg", "Server accepts anything", "Policies + pipelines are the real gate"],
                ],
                "widths": [42, 70, 70],
            },
        ],
        "one_liner": "Hooks are the bouncer at the commit club — ugly messages do not get past the velvet rope.",
        "lab_intro": "Personal clone. Client-side hook for this lab. Azure Repos will not run .git/hooks for you.",
        "lab": [
            "From repo root: copy the sample commit-msg to .git/hooks/commit-msg (Windows Git Bash: the hooks folder under .git).",
            "On Windows, if the file is not executable, run the hook via Git Bash. Keep LF line endings.",
            "git switch -c feature/day16-hooks && echo test > notes/day16.md && git add notes/day16.md",
            "git commit -m \"bad message\" → expect the hook to reject (no type prefix).",
            "git commit -m \"docs: day 16 hook notes\" → expect success.",
            "Optional: add .pre-commit-config.yaml with repo: pre-commit-hooks, hook trailing-whitespace; pip install pre-commit && pre-commit install.",
            "Push the markdown + any pre-commit config. Do not commit .git/hooks (it is not in the tree).",
        ],
        "code_title": "commit-msg hook — require conventional prefix (lab only)",
        "code": "#!/bin/sh\n# save as .git/hooks/commit-msg  (not committed)\ngrep -qE '^(docs|feat|fix|chore|test|refactor)(\\(.+\\))?: .+' \"$1\" || {\n  echo \"Commit message must look like: docs: short description\"\n  exit 1\n}\n",
        "checklist": [
            "Can explain client hook vs branch policy vs pipeline check",
            "A bad commit message was rejected by commit-msg",
            "A conventional message succeeded and was pushed",
            "Understood --no-verify bypasses client hooks",
        ],
        "tomorrow": "Fork workflows & permissions",
    },
    17: {
        "topic": "Fork Workflows & Repo Permissions",
        "subtitle": "Permissions are seatbelts — annoying until someone force-pushes main into the sun",
        "phase": "2 - Azure Repos & Git Mastery",
        "tables": [
            {
                "title": "Architecture A — identity map (who can touch which ref)",
                "columns": ["Identity", "Typical Git rights", "Lab setting"],
                "rows": [
                    ["Project Collection Admin", "Bypass almost everything", "You only"],
                    ["Project Administrators", "Repo create/delete, security", "You"],
                    ["Contributors", "Contribute (push to non-protected), PR", "Default if a friend joins"],
                    ["Readers", "Clone / pull", "No push"],
                    ["<Repo> Bypass policies", "Push to main ignoring PR", "Deny for Contributors"],
                    ["Force push (rewrite)", "git push --force on that ref", "Deny on main for everyone but you-as-breakglass"],
                ],
                "widths": [44, 72, 66],
            },
            {
                "title": "Architecture B — fork vs branch permission (pick the contribution model)",
                "columns": ["Model", "How code arrives", "Use when"],
                "rows": [
                    ["In-repo feature/*", "Same repo, permission on refs", "Trusted small team (this lab)"],
                    ["Fork + PR", "Fork repo in Azure Repos or GitHub; PR across forks", "Open source / untrusted contributors"],
                    ["GitHub mirror", "Second remote; extra origin", "Optional public notes later — not required"],
                    ["TFVC check-in", "Centralized lock", "Out of scope"],
                    ["Public project + Contributors", "Anyone with link may leak", "Keep azure-100-labs private"],
                    ["Bypass for 'just this once'", "Direct push to main", "Treat as an incident, not a workflow"],
                ],
                "widths": [40, 72, 70],
            },
            {
                "title": "Architecture C — pitfalls that look like 'Git is broken'",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["Force push allowed on main", "git push --force origin main succeeds", "Repos → Security → Deny Force push (main)"],
                    ["Bypass policies = true for Contributors", "PR required checkbox is theatre", "Deny Bypass policies on the ref"],
                    ["Fork of a work repo into personal", "Employer code on your MSA", "Never. Lab uses only your commits"],
                    ["Inherited allow + deny confusion", "Effective = Allow when you thought Deny", "Read the Effective column, not just the row you edited"],
                    ["Guest with Project Admin", "They can rewrite security", "Audit Groups; remove guests"],
                    ["Relying on .git/hooks to stop force push", "Other clone still force-pushes", "Server ACL + Day 19 policies"],
                ],
                "widths": [48, 66, 68],
            },
        ],
        "one_liner": "Permissions are seatbelts — annoying until the day someone force-pushes main into the sun.",
        "lab_intro": "Personal org Project settings only. Document the ACL. Invite nobody from work. Forks optional.",
        "lab": [
            "Project settings → Repositories → select azure-100-labs → Security.",
            "Select Contributors. Find Force push. Set Deny (or confirm inherited Deny) for this repo / refs/heads/main if scoped.",
            "Find Bypass policies when pushing / Bypass policies when completing pull requests. Deny for Contributors.",
            "Confirm your user (as Project Admin) still can administer. Do not add a work AAD group.",
            "Write docs/repo-permissions-day17.md: Contributors may contribute via PR; cannot force-push main; cannot bypass policies.",
            "Optional: GitHub → new private repo → add remote github and push notes only. Never mirror a work repository.",
            "Commit the markdown on feature/day17-permissions and open a PR. Do not screenshot employer orgs.",
        ],
        "code_title": "Notes — repo Security click path (no REST required today)",
        "code": "# Project Settings → Repositories → azure-100-labs → Security\n# Contributors:\n#   Force push = Deny\n#   Bypass policies when pushing = Deny\n#   Bypass policies when completing pull requests = Deny\n# Readers: no Contribute\n# Documented in docs/repo-permissions-day17.md\n",
        "checklist": [
            "Can explain fork workflow vs in-repo feature branches",
            "Contributors cannot force-push main (Deny)",
            "Bypass policies denied for Contributors",
            "docs/repo-permissions-day17.md committed on the personal repo",
        ],
        "tomorrow": "Migrating repos to Azure Repos",
    },
    18: {
        "topic": "Migrating Repos to Azure Repos",
        "subtitle": "Moving apartments — bring the furniture (history), leave the broken IKEA (secrets)",
        "phase": "2 - Azure Repos & Git Mastery",
        "tables": [
            {
                "title": "Architecture A — import paths (history is commits, not ZIP files)",
                "columns": ["Path", "What is preserved", "Lab choice"],
                "rows": [
                    ["ADO Import (Repos → Import)", "Git history from a public URL you can reach", "Small public GitHub sample"],
                    ["git clone --bare + push --mirror", "All refs: branches, tags", "When Import UI cannot see the source"],
                    ["git remote add + push --all --tags", "Named branches you pick", "When you do not want every junk ref"],
                    ["TFVC import", "Changesets → Git (lossy-ish)", "Mention only — stay on Git"],
                    ["ZIP upload", "One tree, no history", "Not a migration"],
                    ["LFS objects", "Pointers unless LFS migrated", "Skip LFS for this lab sample"],
                ],
                "widths": [52, 70, 60],
            },
            {
                "title": "Architecture B — decide what NOT to bring",
                "columns": ["Object", "Bring?", "Why"],
                "rows": [
                    ["Commit DAG + tags", "Yes", "git log must show old authors/dates"],
                    ["main / master history", "Yes, then set default main", "Pipelines trigger on the default"],
                    [".env / PATs in old blobs", "No — rewrite or leave behind", "Rotate every secret that ever lived in Git"],
                    ["node_modules from old mistakes", "No", "BFG / git filter-repo if already imported"],
                    ["Work remote URL in README", "No", "Rewrite clone instructions to personal ADO"],
                    ["Wiki / Boards", "Separate", "Import is Git only; work items do not ride along"],
                ],
                "widths": [44, 40, 98],
            },
            {
                "title": "Architecture C — pitfalls that look like 'history vanished'",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["Push without --mirror, missing tags", "git tag empty on ADO", "git push origin --tags or redo --mirror"],
                    ["Import of a huge monorepo", "UI timeout / LFS 404", "Pick a tiny public repo for the lab"],
                    ["Default branch still empty main", "Imported code on master only", "Repos → Branches → set default; delete empty main or merge"],
                    ["Auth during Import", "Cannot fetch private GitHub", "Use --bare from a machine that already has access — still no work IP"],
                    ["Force-push mirror onto a repo with PRs", "Rewrote SHAs under existing PRs", "Import into a NEW empty repo imported-sample"],
                    ["Secrets survived", "git log -S 'AKIA' still hits", "filter-repo + rotate; do not publish"],
                ],
                "widths": [44, 68, 70],
            },
        ],
        "one_liner": "Migrations are moving apartments — history is the furniture; leave the broken IKEA shelf (secrets) behind.",
        "lab_intro": "Personal project azure-100-labs. Create a NEW empty repo imported-sample. Do not import employer code.",
        "lab": [
            "Repos → New repository → Name imported-sample → do NOT add a README (empty, ready for import).",
            "Option A: imported-sample → Import → paste a SMALL public GitHub URL you own or a well-known tiny sample. Import.",
            "Option B (if Import fails): git clone --bare https://github.com/<user>/<small-repo>.git then git push --mirror https://dev.azure.com/<org>/azure-100-labs/_git/imported-sample",
            "Clone imported-sample locally. git log --oneline -10. Confirm old commits (dates/messages) survived.",
            "Repos → Branches: set default to main if the source used main; if only master exists, leave it or rename.",
            "Search the tree for .env, id_rsa, nuget.config with keys. If found, document in notes — do not push fixes to a work repo.",
            "Write docs/migration-day18.md in azure-100-labs (the main repo): which option you used and that git log still shows history.",
        ],
        "code_title": "Mirror a public Git history into an empty Azure Repos repo",
        "code": "git clone --bare https://github.com/<user>/<small-public-repo>.git\ncd <small-public-repo>.git\ngit push --mirror https://dev.azure.com/<org>/azure-100-labs/_git/imported-sample\ncd ..\ngit clone https://dev.azure.com/<org>/azure-100-labs/_git/imported-sample\ncd imported-sample\ngit log --oneline --decorate -10\n",
        "checklist": [
            "imported-sample exists in the personal project",
            "git log shows commits from before the import",
            "Did not import employer / private work history",
            "Documented Import UI vs --mirror in the main lab repo",
        ],
        "tomorrow": "Repo security",
    },
    19: {
        "topic": "Repo Security",
        "subtitle": "Branch policies are parental controls for adults who still push to main at 11:58pm",
        "phase": "2 - Azure Repos & Git Mastery",
        "tables": [
            {
                "title": "Architecture A — policy stack on refs/heads/main",
                "columns": ["Policy", "Azure Repos setting", "Lab value"],
                "rows": [
                    ["Minimum reviewers", "Require a minimum number of reviewers", "1 (yourself OK)"],
                    ["Linked work items", "Check for linked work items", "Required"],
                    ["Merge types", "Limit merge types", "Allow squash only"],
                    ["Comment resolution", "Check for comment resolution", "Optional today; on in real teams"],
                    ["Build validation", "Build pipeline must succeed", "Add after Day 21 YAML exists"],
                    ["Status checks", "External GitHub/ADO statuses", "Skip unless you add one"],
                    ["Reset on push / stale reviews", "Require approval on last iteration", "On if you want serious review"],
                ],
                "widths": [40, 72, 70],
            },
            {
                "title": "Architecture B — decide required vs optional (constraint, not vibes)",
                "columns": ["Constraint", "Required?", "Skip when"],
                "rows": [
                    ["Stop git push origin main", "Yes — require PR + deny Contribute on main or policy", "Never on this lab after today"],
                    ["Traceability for DORA / audit", "Linked work item Required", "Throwaway branches not merging to main"],
                    ["Linear history", "Squash only", "You truly need merge commits for two parents"],
                    ["CI as a gate", "Build validation on azure-pipelines.yml", "YAML does not exist yet (Day 21)"],
                    ["Two-person integrity", "2 reviewers", "Solo personal lab — 1 is enough"],
                    ["Admin bypass", "Keep PCA bypass for breakglass", "Do not use bypass for daily commits"],
                ],
                "widths": [48, 70, 64],
            },
            {
                "title": "Architecture C — pitfalls that queue PRs or still allow a 11:58pm push",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["Policy on develop, not main", "main still accepts git push", "Branch policies on the default branch"],
                    ["Reviewers = 1 but you auto-approve without looking", "Green checkbox, unread diff", "Use Files tab; optional: prohibit requestor vote"],
                    ["All merge types allowed", "Merge commit + rebase mix on main", "Limit to squash as per ADR"],
                    ["Work item optional", "PRs with no AB#", "Set Check for linked work items = Required"],
                    ["Bypass policies permission", "Direct push succeeds for Contributors", "Day 17 Deny; re-check Effective rights"],
                    ["Build validation pointing at Classic", "Wrong pipeline, always skipped", "Wait for YAML; then pick the right definition"],
                ],
                "widths": [44, 68, 70],
            },
        ],
        "one_liner": "Branch policies are parental controls for adults who still push to main at 11:58pm.",
        "lab_intro": "Personal azure-100-labs. Harden main. Then prove a direct push fails. No work project policies.",
        "lab": [
            "Repos → Branches → … on main → Branch policies.",
            "Require a minimum number of reviewers: 1. Allow requestors to approve their own changes (solo lab).",
            "Check for linked work items: Required.",
            "Limit merge types: uncheck Merge / Rebase / Semi-linear; leave Squash merge checked.",
            "Save. Locally: git switch main && echo bypass > notes/should-fail.txt && git add && git commit -m \"docs: should be blocked\" && git push origin main",
            "Expect rejected by policy. Delete the local commit: git reset --hard origin/main (only if push failed and commit is local).",
            "Open a PR from a feature branch with a linked work item and squash-complete it to prove the happy path.",
        ],
        "code_title": "Prove direct push to main is rejected (then reset local)",
        "code": "git switch main\ngit pull origin main\necho blocked > notes/should-fail.txt\ngit add notes/should-fail.txt\ngit commit -m \"docs: should be blocked by branch policy\"\ngit push origin main\n# TF401027 / TF402455 / 'rejected by policy' — success for this lab\ngit reset --hard origin/main\n",
        "checklist": [
            "main requires 1 reviewer and a linked work item",
            "Merge types limited to squash",
            "Direct git push origin main failed",
            "A compliant PR still merges",
        ],
        "tomorrow": "Phase 2 mini project",
    },
    20: {
        "topic": "Mini Project + Recap (Phase 2)",
        "subtitle": "Branch, PR, policy, squash — Git with receipts, not 'save file'",
        "phase": "2 - Azure Repos & Git Mastery",
        "tables": [
            {
                "title": "Architecture A — Phase 2 system map (what 'done' means)",
                "columns": ["Capability", "Days", "Proof"],
                "rows": [
                    ["Four Git areas + objects", "11", "notes/day11.md + git log"],
                    ["Branching ADR", "12", "docs/branching-strategy.md = GitHub Flow"],
                    ["Layout + gitignore", "13", "/src /docs /pipelines /infra"],
                    ["PR template + AB#", "14", ".azuredevops/pull_request_template.md"],
                    ["Rebase/conflict literacy", "15", "Resolved conflict; no force on main"],
                    ["Client hooks vs policies", "16–19", "commit-msg + main policies"],
                    ["Import + ACL", "17–18", "imported-sample + Deny force push"],
                ],
                "widths": [44, 28, 110],
            },
            {
                "title": "Architecture B — the path a change must take after today",
                "columns": ["Step", "Command / UI", "Gate"],
                "rows": [
                    ["Branch", "git switch -c feature/day20-recap", "Never commit on main"],
                    ["Commit", "conventional subject", "Optional client commit-msg"],
                    ["Push", "git push -u origin HEAD", "ACL: Contribute on feature refs"],
                    ["PR", "template + AB# + reviewers", "Policies on main"],
                    ["Squash complete", "Delete source branch", "Limit merge types"],
                    ["Default branch", "main SHA updated", "Pipeline will trigger here in Phase 3"],
                ],
                "widths": [32, 78, 72],
            },
            {
                "title": "Architecture C — pitfalls that fake a Phase 2 recap",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["Merged by bypass", "PCA bypassed policies 'to save time'", "Undo with a proper PR; document the miss"],
                    ["README still empty of workflow", "Clone instructions only", "Add How we use Git here pointing at the ADR"],
                    ["Policy never blocked anything", "You never tried git push main", "Repeat Day 19 proof; keep the error text"],
                    ["Feature branch left forever", "stale feature/day11-*", "Delete after squash; Repos → Stale branches"],
                    ["Work Git history in imported-sample", "Employer file paths", "Delete that repo; start a public sample"],
                    ["No DORA proxy yet", "Lead time still vibes", "Tomorrow pipelines give timestamps"],
                ],
                "widths": [42, 70, 70],
            },
        ],
        "one_liner": "Phase 2 recap: Git is not 'save file' — it is collaboration with receipts, policies, and fewer 2am disasters.",
        "lab_intro": "Personal azure-100-labs. Run the full path once: branch → commit → PR → squash → delete branch. Policies must have blocked a bad PR or push.",
        "lab": [
            "Confirm Branch policies on main (reviewer, work item, squash). If missing, restore Day 19.",
            "git switch main && git pull && git switch -c feature/day20-recap",
            "Append README section How we use Git here with a link to docs/branching-strategy.md",
            "git add README.md && git commit -m \"docs: document git workflow\" && git push -u origin feature/day20-recap",
            "Open PR → fill template → link a User Story → squash complete → tick delete source branch.",
            "Try a non-compliant PR (no work item) or push to main; confirm it is blocked. Close/abandon the bad PR.",
            "Repos → Branches: stale feature/* deleted. Update Boards story to Done.",
        ],
        "code_title": "End-to-end feature branch for the Phase 2 recap",
        "code": "git switch main\ngit pull origin main\ngit switch -c feature/day20-recap\nprintf '\\n## How we use Git here\\nSee docs/branching-strategy.md\\n' >> README.md\ngit add README.md\ngit commit -m \"docs: document git workflow\"\ngit push -u origin feature/day20-recap\n# Azure Repos: PR → squash merge → delete feature/day20-recap\n",
        "checklist": [
            "Can describe the required path to main without saying 'just push'",
            "README documents the Git workflow + ADR",
            "A non-compliant PR or direct push was blocked once",
            "Happy-path squash merge completed; source branch deleted",
        ],
        "tomorrow": "Intro to Azure Pipelines",
    },
    21: {
        "topic": "Intro to Azure Pipelines",
        "subtitle": "A robot that runs the build so you can stop being the robot",
        "phase": "3 - Continuous Integration",
        "tables": [
            {
                "title": "Architecture A — pipeline run graph (YAML file → logs)",
                "columns": ["Piece", "YAML / product", "What it does"],
                "rows": [
                    ["Pipeline definition", "azure-pipelines.yml in Git", "Versioned with the code"],
                    ["Trigger", "trigger: branches include main", "Start a run on push"],
                    ["Pool", "pool.vmImage: ubuntu-latest", "Microsoft-hosted agent VM"],
                    ["Job", "jobs.job (implicit job if only steps)", "Unit of agent allocation"],
                    ["Step", "script / task / checkout", "Process on the agent"],
                    ["Run / logs", "Pipelines → Runs → Job log", "The product you actually debug"],
                    ["Parallel job SKU", "Org settings → Parallel jobs", "Without it, runs queue forever"],
                ],
                "widths": [36, 72, 74],
            },
            {
                "title": "Architecture B — YAML vs Classic (decide once for this series)",
                "columns": ["Need", "Default", "Classic only if"],
                "rows": [
                    ["Reviewable CI", "YAML in /pipelines or repo root", "You are maintaining a legacy GUI release"],
                    ["Branch diffs", "git blame the YAML", "Classic JSON in ADO, not in Git"],
                    ["Reuse", "templates: later days", "Task groups in Classic"],
                    ["PR validation", "pr: + branch policy build validation", "Classic build attached as policy"],
                    ["This 100-day lab", "YAML only", "Do not create a Classic build 'to compare' beyond a look"],
                    ["Disable Classic", "Org/project: Disable creation of classic pipelines (if present)", "Admin toggle — optional discipline"],
                ],
                "widths": [36, 78, 68],
            },
            {
                "title": "Architecture C — pitfalls that never start a run",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["YAML not in default branch", "Pipeline exists, push to main does nothing", "Merge YAML to main; trigger includes main"],
                    ["Wrong repo", "Pipeline bound to day05-overview", "Create pipeline on azure-100-labs"],
                    ["No hosted parallel job", "Waiting for an available agent", "Org settings → Parallel jobs; free Microsoft-hosted"],
                    ["pool: Default by copy-paste", "Job queued on empty self-hosted pool", "pool: vmImage: ubuntu-latest"],
                    ["Classic editor curiosity", "GUI pipeline with no Git history", "Delete it; keep YAML"],
                    ["Checkout skipped + script needs files", "cat: No such file", "steps include implicit checkout unless checkout: none"],
                ],
                "widths": [42, 70, 70],
            },
        ],
        "one_liner": "Pipelines are robots that run your build so you can stop being the robot.",
        "lab_intro": "Personal project azure-100-labs. Microsoft-hosted ubuntu-latest only. No Classic definition as the source of truth.",
        "lab": [
            "Pipelines → Create Pipeline → Azure Repos Git → azure-100-labs → Starter pipeline.",
            "Replace the body with the YAML in this handout. Save as /azure-pipelines.yml (repo root) on a feature branch or main via PR.",
            "Run pipeline. Open the job log. Find the image / Agent name / Agent.OS lines — that is hosted proof.",
            "Confirm YAML has pool: vmImage: ubuntu-latest (not pool: Default).",
            "Org settings → Agent pools / Parallel jobs. Note Microsoft-hosted vs any Default pool. Do not register an agent today.",
            "Optional: Project settings → Pipelines → Settings → disable creation of classic build/release if the toggle exists.",
            "PR the YAML if main is policy-protected. Keep the first green run ID in notes.",
        ],
        "code_title": "azure-pipelines.yml — hello on Microsoft-hosted ubuntu-latest",
        "code": "trigger:\n  branches:\n    include:\n      - main\n\npool:\n  vmImage: ubuntu-latest\n\nsteps:\n  - checkout: self\n  - script: |\n      echo Hello from Day 21\n      echo Agent.OS=$(Agent.OS)\n      echo Agent.Name=$(Agent.Name)\n      echo Build.SourceVersion=$(Build.SourceVersion)\n    displayName: Hello and prove the agent\n",
        "checklist": [
            "Can name trigger, pool, job, step, run log",
            "Green run on ubuntu-latest with Hello in the log",
            "YAML lives in Git (not only Classic GUI)",
            "Did not register a self-hosted agent",
        ],
        "tomorrow": "Microsoft-hosted vs self-hosted agents",
    },
    22: {
        "topic": "Microsoft-hosted vs Self-hosted Agents",
        "subtitle": "Pick the agent like a capacity decision, not a personality trait",
        "phase": "3 - Continuous Integration",
        "tables": [
            {
                "title": "Architecture A — hosted vs self-hosted (same YAML job, different product)",
                "columns": ["Dimension", "Microsoft-hosted", "Self-hosted"],
                "rows": [
                    ["What it is", "Microsoft-provided VM image for the job", "A machine / scale set you operate"],
                    ["YAML", "pool: vmImage: ubuntu-latest", "pool: Default (named pool)"],
                    ["Images", "ubuntu-latest, windows-latest, macOS SKUs with SDKs on the image", "Whatever you installed. You own drift."],
                    ["Patching / disk", "Microsoft patches the VM. Workspace dies with the job", "You patch OS, agents, AV, certs. Disk fills with leftover workspaces"],
                    ["Network", "Public Microsoft IPs. No hop into your VNet unless you design for it", "Can sit on a VNet / on-prem. Use when the job must reach private endpoints"],
                    ["Licensed tools", "Public SDKs only. No ISV compiler that cannot leave the building", "Install the licensed compiler, private NuGet, air-gapped feed"],
                    ["Failure mode", "Queue delay / hosted outage / image SKU change", "Agent offline, capability mismatch, scale-set at 0, 2am patch window"],
                    ["Bill", "Microsoft-hosted minutes + parallel-job SKU", "Parallel-job SKU PLUS the VM / scale set. Two bills, not one"],
                ],
                "widths": [32, 75, 75],
            },
            {
                "title": "Architecture B — decide with a constraint, not a blog title",
                "columns": ["Constraint", "Default", "Move off hosted when"],
                "rows": [
                    ["Public SaaS build", "Hosted ubuntu-latest", "Never, unless a later constraint appears"],
                    ["Private network hop", "Hosted cannot see it", "Job must hit a VNet-only feed, private AKS API, or on-prem artifact share"],
                    ["Licensed / air-gapped tool", "Not on the Microsoft image", "Compiler, scanner, or SDK that policy forbids on a public VM"],
                    ["Mac / Windows SKU need", "Use the hosted image first", "Image lacks the SDK pin you actually build with — then pin or self-host"],
                    ["Control as a feeling", "Still hosted", "We want control is not a constraint. Offline agents at 2am is the cost of that feeling"],
                ],
                "widths": [42, 58, 82],
            },
            {
                "title": "Architecture C — pitfalls that queue jobs forever",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["Capability folklore", "Job waits on a pool named the Java agent", "Demand a registered capability in YAML, not a nickname in Slack"],
                    ["Pool sprawl", "Five pools, nobody knows which one is live", "One pool per real constraint (OS / network / license). Delete the rest"],
                    ["Missing parallel job", "Self-hosted VM is up; pipeline still queued", "Self-hosted still needs an Azure DevOps parallel job. The VM bill is extra"],
                    ["Agent version drift", "Worked Friday, fails Monday after an agent update", "Pin agent version on scale sets. Treat agents as cattle with a known image"],
                    ["Workspace leftovers", "Disk 100%, jobs fail with no obvious YAML error", "Clean workspaces; prefer scale-set agents that die with the job"],
                ],
                "widths": [40, 71, 71],
            },
        ],
        "one_liner": "Hosted is Uber. Self-hosted is owning the car — insurance, parking, and the 2am flat tyre included.",
        "lab_intro": "Personal Azure DevOps org only. Project azure-100-labs (or today's lab project).",
        "lab": [
            "Open Microsoft Learn: hosted vs self-hosted agents. Write three constraints that would force self-hosted (private network, licensed tool, policy). If you cannot name one, hosted stays the default.",
            "Org settings → Agent pools. List every pool. Note Microsoft-hosted vs any Default / self-hosted pool. Screenshot nothing from an employer org.",
            "Open yesterday's YAML (Day 21). Confirm pool: vmImage: ubuntu-latest. That is the rule for every remaining 100-day lab unless a private-network requirement appears.",
            "In the last pipeline run, open the job log. Find the image name / hosted agent line. That receipt is how you prove hosted actually ran.",
            "Write docs/agents-day22.md with: (1) the three constraints, (2) the pool list from this org, (3) the one-sentence rule you will follow for Days 23–100.",
            "Do not install the agent on your laptop 'for practice'. That creates a 2am offline pool you now own.",
        ],
        "code_title": "Starter YAML — hosted stays the default",
        "code": "trigger:\n  - main\npool:\n  vmImage: ubuntu-latest  # Microsoft-hosted. Do not switch to a named pool for labs.\nsteps:\n  - script: echo Agent image is $(Agent.OS) / $(Agent.Name)\n    displayName: Prove which agent ran\n",
        "checklist": [
            "Can explain hosted vs self-hosted without saying we wanted control",
            "Named at least one real constraint that would justify self-hosted — or wrote none yet",
            "YAML still uses vmImage: ubuntu-latest",
            "docs/agents-day22.md exists on the personal repo",
        ],
        "tomorrow": "YAML pipeline basics",
    },
    23: {
        "topic": "YAML Pipeline Basics",
        "subtitle": "trigger, pr, stages, jobs, steps — Lego instructions that care about whitespace",
        "phase": "3 - Continuous Integration",
        "tables": [
            {
                "title": "Architecture A — YAML schema map (outermost key to leaf)",
                "columns": ["Key", "Meaning", "Lab value"],
                "rows": [
                    ["trigger", "CI on push to listed branches", "branches.include: [main]"],
                    ["pr", "Validation on PRs targeting listed branches", "branches.include: [main]"],
                    ["stages", "Ordered groups; default one implicit stage", "Build then Test"],
                    ["jobs", "Agent allocation unit; can dependOn", "BuildJob, TestJob"],
                    ["pool", "vmImage or name", "ubuntu-latest on each job"],
                    ["steps", "script, bash, pwsh, task, checkout, template", "echo is enough today"],
                    ["dependsOn", "Stage/job graph", "Test dependsOn Build"],
                    ["variables", "compile-time vs runtime (Day 29)", "Skip secrets today"],
                ],
                "widths": [32, 78, 72],
            },
            {
                "title": "Architecture B — decide stage vs job vs step",
                "columns": ["Need", "Use", "Do not"],
                "rows": [
                    ["Different agent SKU / approval later", "stage + job pool", "One 40-step job named Deploy"],
                    ["Share a workspace / checkout", "steps in the SAME job", "dependsOn across jobs and expect files to be there"],
                    ["Fail-fast compile then tests", "stage Test dependsOn Build", "continueOnError: true on Build 'so Test can run'"],
                    ["PR only vs main only", "pr: and trigger: separately", "Assume trigger covers PRs — it does not"],
                    ["Reuse", "template: later", "Copy-paste 8 YAML files by hand"],
                    ["Logs as the product", "displayName on every step", "Untitled script steps you cannot find"],
                ],
                "widths": [50, 62, 70],
            },
            {
                "title": "Architecture C — pitfalls that skip PR validation or lose the workspace",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["trigger: none accidentally", "Manual run only", "Restore trigger.branches.include"],
                    ["No pr: key", "PR has no Checks; merge is opinion", "Add pr: branches include main; then branch policy"],
                    ["Job2 needs Job1 binaries", "file not found on the second agent", "PublishPipelineArtifact@1 / download, or same job"],
                    ["Tab vs space", "Mapping values are not allowed here", "Spaces only; VS Code YAML + schema"],
                    ["pool only at pipeline root, job overrides missing", "One job silently on Default", "Set pool on each job while learning"],
                    ["Skipped logs", "You merge because the badge is green", "Open the Test stage log end-to-end once"],
                ],
                "widths": [44, 68, 70],
            },
        ],
        "one_liner": "YAML pipelines are Lego instructions written by someone who enjoys whitespace arguments.",
        "lab_intro": "Personal azure-100-labs. Expand Day 21 hello into stages. Add a PR trigger. Read the logs. Hosted ubuntu-latest only.",
        "lab": [
            "Edit azure-pipelines.yml (or pipelines/ci.yml and retarget the pipeline) on feature/day23-yaml.",
            "Add trigger.branches.include: [main] AND pr.branches.include: [main].",
            "Add stages: Build (echo Building) then Test dependsOn: Build (echo Testing). Each job pool: vmImage: ubuntu-latest.",
            "Push and open a PR to main. Confirm the pipeline appears on the PR Checks tab.",
            "Pipelines → latest run → expand both stages. Read every step log. Note stage names in the graph.",
            "If policies require a work item, link one. Squash merge when green.",
            "Do not add a self-hosted pool name. Do not add secrets.",
        ],
        "code_title": "Two-stage YAML with CI + PR triggers",
        "code": "trigger:\n  branches:\n    include:\n      - main\npr:\n  branches:\n    include:\n      - main\n\nstages:\n- stage: Build\n  jobs:\n  - job: BuildJob\n    pool:\n      vmImage: ubuntu-latest\n    steps:\n    - script: echo Building...\n      displayName: Compile stub\n\n- stage: Test\n  dependsOn: Build\n  jobs:\n  - job: TestJob\n    pool:\n      vmImage: ubuntu-latest\n    steps:\n    - script: echo Testing...\n      displayName: Test stub\n",
        "checklist": [
            "Can name trigger, pr, stage, job, step, dependsOn",
            "PR to main queued a validation run",
            "Test stage ran after Build in the logs",
            "Both jobs used ubuntu-latest",
        ],
        "tomorrow": "CI pipeline for a .NET app",
    },
    24: {
        "topic": "CI Pipeline for a .NET App",
        "subtitle": "restore, build, test, publish — skip one and production finds it",
        "phase": "3 - Continuous Integration",
        "tables": [
            {
                "title": "Architecture A — .NET CI path on a hosted agent",
                "columns": ["Step", "Command / task", "Output"],
                "rows": [
                    ["SDK pin", "UseDotNet@2 packageType sdk version 8.x", "dotnet --version on ubuntu-latest"],
                    ["Restore", "dotnet restore src/SampleApi/SampleApi.csproj", "NuGet assets; can use day09-packages later"],
                    ["Build", "dotnet build -c Release --no-restore", "bin/Release/net8.0"],
                    ["Test", "dotnet test -c Release --no-build --logger trx", "trx on Test Results tab if published"],
                    ["Publish app", "dotnet publish -c Release -o $(Build.ArtifactStagingDirectory)/api", "Runnable output, not the full src tree"],
                    ["Publish artifact", "PublishPipelineArtifact@1 artifactName drop", "Consumable by a later deploy stage"],
                    ["Trigger filter", "trigger.paths.include: src/SampleApi/**", "Do not rebuild on docs-only commits"],
                ],
                "widths": [32, 82, 68],
            },
            {
                "title": "Architecture B — decide SDK, configuration, artifact shape",
                "columns": ["Choice", "Lab default", "Change when"],
                "rows": [
                    ["SDK", "8.x via UseDotNet@2 (not whatever the image has today)", "You pin 9.x globally.json to match"],
                    ["Configuration", "Release for CI", "Debug only for local F5"],
                    ["Artifact", "Pipeline artifact drop of publish output", "NuGet pack to Azure Artifacts (Day 9 feed) later"],
                    ["Test fail", "Fail the job (no || true)", "You are hiding a red build"],
                    ["windows-latest vs ubuntu", "ubuntu-latest for this API", "You need Windows-only workloads (WPF, some SDKs)"],
                    ["Restore source", "nuget.org (hosted)", "Private feed: NuGetAuthenticate@1 + nuget.config"],
                ],
                "widths": [32, 82, 68],
            },
            {
                "title": "Architecture C — pitfalls that publish bin/ instead of the app",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["PublishBuildArtifacts of bin/Release", "Includes obj noise, missing deps.json layout", "dotnet publish -o staging then artifact that folder"],
                    ["|| true on dotnet test", "Green badge, failing tests", "Remove || true; fix tests"],
                    ["No UseDotNet@2", "SDK mismatch vs global.json", "Pin 8.x (or your pin) on the job"],
                    ["Path trigger too wide", "README typo burns hosted minutes", "paths.include src/SampleApi/**"],
                    ["Restore without --no-restore later", "Double restore, slower CI", "restore once; build --no-restore"],
                    ["Checking in bin/", "PRs unreviewable", "Day 13 gitignore; git rm --cached"],
                ],
                "widths": [44, 68, 70],
            },
        ],
        "one_liner": ".NET CI is the same ritual every time: restore, build, test, publish — skip one and production finds it for you.",
        "lab_intro": "Personal azure-100-labs. Create a tiny Web API under /src/SampleApi. Microsoft-hosted ubuntu-latest. No work NuGet feeds.",
        "lab": [
            "Local SDK 8: dotnet new webapi -n SampleApi -o src/SampleApi && dotnet new xunit -n SampleApi.Tests -o src/SampleApi.Tests && dotnet add src/SampleApi.Tests/SampleApi.Tests.csproj reference src/SampleApi/SampleApi.csproj. One Assert.True is enough.",
            "Commit on feature/day24-dotnet-ci. Do not commit bin/ obj/.",
            "Add pipelines/dotnet-ci.yml (code below). Pipelines → New pipeline → Existing YAML → this file. Or extend azure-pipelines.yml with a path filter.",
            "Push a PR. Confirm UseDotNet@2 logs SDK 8.x, restore, build, test, publish artifact drop.",
            "Open the run → Artifacts → drop. Confirm published output, not the whole repo.",
            "If tests were skipped, add at least one test project before calling CI done. Do not leave || true.",
            "Squash merge when green. Hosted pool only.",
        ],
        "code_title": "pipelines/dotnet-ci.yml — restore, build, test, publish drop",
        "code": "trigger:\n  branches:\n    include:\n      - main\n  paths:\n    include:\n      - src/SampleApi/**\n      - src/SampleApi.Tests/**\n      - pipelines/dotnet-ci.yml\npr:\n  branches:\n    include:\n      - main\n  paths:\n    include:\n      - src/SampleApi/**\n      - src/SampleApi.Tests/**\n      - pipelines/dotnet-ci.yml\n\npool:\n  vmImage: ubuntu-latest\n\nsteps:\n- task: UseDotNet@2\n  inputs:\n    packageType: sdk\n    version: 8.x\n\n- script: |\n    dotnet restore src/SampleApi.Tests/SampleApi.Tests.csproj\n    dotnet build src/SampleApi.Tests/SampleApi.Tests.csproj -c Release --no-restore\n    dotnet test src/SampleApi.Tests/SampleApi.Tests.csproj -c Release --no-build --logger trx\n    dotnet publish src/SampleApi/SampleApi.csproj -c Release --no-build -o $(Build.ArtifactStagingDirectory)/api\n  displayName: restore, build, test, publish\n\n- task: PublishPipelineArtifact@1\n  inputs:\n    targetPath: $(Build.ArtifactStagingDirectory)/api\n    artifact: drop\n",
        "checklist": [
            "src/SampleApi exists and builds on ubuntu-latest",
            "Pipeline pins SDK 8.x with UseDotNet@2",
            "Artifact drop is publish output, not raw bin/",
            "No || true hiding test failures — red tests fail the job",
        ],
        "tomorrow": "CI for Node.js",
    },
    25: {
        "topic": "CI Pipeline for a Node.js App",
        "subtitle": "npm ci + Cache@2 — stop paying hosted minutes to watch paint dry",
        "phase": "3 - Continuous Integration",
        "tables": [
            {
                "title": "Architecture A — Node CI path (lockfile is the contract)",
                "columns": ["Step", "Task / command", "Why"],
                "rows": [
                    ["Runtime pin", "NodeTool@0 versionSpec 20.x", "Not whatever ubuntu-latest shipped this week"],
                    ["Cache key", "Cache@2 key: npm | $(Agent.OS) | package-lock.json", "Restore node_modules when lockfile unchanged"],
                    ["Install", "npm ci (not npm install)", "Fails if lockfile is out of sync — that is a feature"],
                    ["Test", "npm test", "Even a one-line Node assert counts"],
                    ["Optional build", "npm run build if you have it", "Skip if the lab app is a script"],
                    ["Lockfile", "package-lock.json committed", "Cache@2 key is useless without it"],
                    ["Path trigger", "src/sample-node/**", "Docs-only commits should not install npm"],
                ],
                "widths": [32, 90, 60],
            },
            {
                "title": "Architecture B — npm ci vs install vs cache",
                "columns": ["Choice", "Lab default", "Avoid when"],
                "rows": [
                    ["npm ci", "Always in CI", "You do not have package-lock.json yet — generate and commit it"],
                    ["npm install", "Local laptop only", "CI: it rewrites the lockfile and hides drift"],
                    ["Cache@2 path node_modules", "Yes, with lockfile in the key", "You cache ~/.npm but still run npm install without ci"],
                    ["Node 20.x", "LTS pin", "Floating node: latest"],
                    ["Private feed", "Later: .npmrc + npmAuthenticate@0 to day09-packages", "PAT in .npmrc committed"],
                    ["windows-latest", "Only if native modules need MSVC", "Default ubuntu-latest"],
                ],
                "widths": [40, 78, 64],
            },
            {
                "title": "Architecture C — pitfalls that miss the cache or ship node_modules",
                "columns": ["Pitfall", "What you see", "Fix"],
                "rows": [
                    ["Cache key without lockfile hash", "Cache never hits, or hits the wrong tree", "Include package-lock.json in Cache@2 key"],
                    ["npm install in CI", "lockfile dirty; 'works on my agent'", "npm ci; commit the lockfile"],
                    ["Committed node_modules", "PR is 10k files; cache pointless", "gitignore + git rm -r --cached node_modules"],
                    ["No restoreKeys", "First build after tiny lock change is cold", "restoreKeys: npm | $(Agent.OS)"],
                    ["Scripts.test missing", "npm ERR! missing script: test", "Add test script even if it is node --test"],
                    ["Cache@2 path wrong", "cache miss every run", "path: src/sample-node/node_modules matching cd"],
                ],
                "widths": [44, 68, 70],
            },
        ],
        "one_liner": "Node CI without caching is watching paint dry while paying Microsoft-hosted minutes for the privilege.",
        "lab_intro": "Personal azure-100-labs. Tiny app under /src/sample-node. Commit package-lock.json. Hosted ubuntu-latest. No work npm registries.",
        "lab": [
            "mkdir src/sample-node && create package.json with name sample-node, scripts.test running node --test or a one-liner. npm install locally to produce package-lock.json.",
            "Confirm .gitignore has node_modules/. Commit package.json + package-lock.json only on feature/day25-node-ci.",
            "Add pipelines/node-ci.yml with NodeTool@0, Cache@2, npm ci, npm test.",
            "Create a pipeline pointing at pipelines/node-ci.yml. Run it twice: first run may be a cache miss; second should hit Cache@2 if lockfile unchanged.",
            "Open the Cache@2 log: look for Cache restored from / Cache saved. That is the receipt.",
            "PR + squash merge. Do not put a PAT in .npmrc.",
            "Optional: Connect to feed day09-packages later — not required for the hello test.",
        ],
        "code_title": "pipelines/node-ci.yml — Node 20, Cache@2, npm ci, npm test",
        "code": "trigger:\n  branches:\n    include:\n      - main\n  paths:\n    include:\n      - src/sample-node/**\n      - pipelines/node-ci.yml\npr:\n  branches:\n    include:\n      - main\n\npool:\n  vmImage: ubuntu-latest\n\nsteps:\n- task: NodeTool@0\n  inputs:\n    versionSpec: 20.x\n\n- task: Cache@2\n  inputs:\n    key: 'npm | \"$(Agent.OS)\" | src/sample-node/package-lock.json'\n    restoreKeys: |\n      npm | \"$(Agent.OS)\"\n    path: src/sample-node/node_modules\n\n- script: |\n    cd src/sample-node\n    npm ci\n    npm test\n  displayName: npm ci and test\n",
        "checklist": [
            "src/sample-node has package.json + package-lock.json; node_modules is gitignored",
            "Pipeline uses NodeTool@0 20.x and npm ci (not npm install)",
            "Cache@2 key includes the lockfile; second run can restore",
            "npm test is green on ubuntu-latest",
        ],
        "tomorrow": "CI for Python",
    },
}
